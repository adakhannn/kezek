# Проблемы и оптимизации проекта Kezek

## Статус документа
- **Дата создания**: 2025-01-XX
- **Последнее обновление**: 2025-02-15
- **Охват**: Весь проект (веб, мобильное приложение, API, база данных)
- **Статус**: ✅ Миграция обработки ошибок завершена, другие оптимизации в процессе

---

## 🔴 Актуальные проблемы

### 1. Критическое: Массовое использование `console.log` в production коде

**Описание**: 
В проекте осталось ~312 использований `console.log/warn/error` в 82 файлах, которые нужно мигрировать на безопасное логирование.

**Риски**:
- Утечка чувствительных данных в логах (токены, ключи, пароли)
- Проблемы с производительностью в продакшене
- Неконтролируемое логирование
- Нарушение безопасности данных

**Рекомендации**:
- Продолжить миграцию критичных компонентов и API routes
- Добавить автоматическую проверку в CI/CD (fail build при наличии console.log)
- Настроить централизованное логирование (Sentry, LogRocket)
- Добавить ESLint правило с ошибкой вместо предупреждения

**Документация:** 
- `apps/web/src/lib/CONSOLE_LOG_MIGRATION.md`
- `MIGRATION_SUMMARY.md`

**Приоритет**: 🔴 **КРИТИЧЕСКИЙ** (безопасность и производительность)

---

### 2. Критическое: Недостаточное покрытие тестами

**Описание**: 
Из ~80+ API endpoints только 24 файла тестов. Многие критичные API endpoints и бизнес-логика не покрыты тестами.

**Текущее покрытие**:
- ✅ Unit-тесты для `financeDomain` (6 файлов)
- ✅ Интеграционные тесты для API endpoints (70 файлов):
  - ✅ Финансовые API (staff/finance, staff/shift/*, dashboard/staff/[id]/finance/stats, dashboard/staff/[id]/finance, dashboard/staff/finance/all, dashboard/finance/all)
  - ✅ Бронирования (bookings/cancel, bookings/mark-attendance)
  - ✅ Уведомления (notify)
  - ✅ CRUD операции (staff/create, staff/update, staff/delete, staff/dismiss, staff/restore, staff/transfer, staff/create-from-user, staff/sync-roles, branches/create, branches/update, branches/delete, branches/schedule, services/create, services/update, services/delete)
  - ✅ Профиль пользователя (profile/update, user/update-phone)
  - ✅ Аватары сотрудников (staff/avatar/upload, staff/avatar/remove)
  - ✅ Dashboard (staff-shifts/[id]/update-hours, staff/[id]/shift/open)
  - ✅ Поиск пользователей (users/search)
  - ✅ Quick booking (quick-book-guest, quick-hold)
  - ✅ Cron задачи (close-shifts)
  - ✅ **Auth endpoints (sign-out, telegram/login, telegram/link, whatsapp/send-otp, whatsapp/verify-otp, whatsapp/create-session, mobile-exchange, yandex/callback)** ✨
  - ✅ **Reviews endpoints (reviews/create, reviews/update)** ✨
  - ✅ **Promotions endpoints (branches/[branchId]/promotions - GET, POST, PATCH, DELETE)** ✨
  - ✅ **Admin endpoints (ratings/status, health-check, initialize-ratings, performance/stats, promotions/debug)** ✨
  - ✅ **WhatsApp endpoints (send-otp, verify-otp, get-phone-numbers, get-business-account, diagnose, test)** ✨
  - ✅ **Webhooks (whatsapp)** ✨
  - ✅ **Метрики (metrics/frontend)** ✨
- ✅ E2E тесты для критичных сценариев (4 файла)
- ⚠️ **Покрытие ~87%** от общего количества endpoints

**Отсутствуют тесты для**:
- Менее критичные endpoints (swagger.json, notify/ping, public/bookings/guest-create, etc.)

**Риски**:
- Высокий риск регрессий при изменениях
- Сложность рефакторинга без уверенности
- Проблемы с качеством кода
- Невозможность безопасного деплоя

**Рекомендации**:
- Добавить тесты для промоакций (promotions)
- Добавить тесты для auth endpoints (sign-in, sign-out, OAuth)
- Добавить тесты для остальных CRUD операций (staff/update, services/update, branches/update)
- Настроить CI/CD с автоматическим запуском тестов и блокировкой деплоя при падении
- Добавить E2E тесты для критичных пользовательских потоков
- Установить минимальный порог покрытия (например, 70%)

**Текущие тесты:** `apps/web/src/__tests__/api/`, `apps/web/src/__tests__/lib/`, `apps/web/e2e/`

**Приоритет**: 🔴 **КРИТИЧЕСКИЙ** (качество и надежность)

---

### 3. Высокий: Непоследовательная обработка ошибок в API

**Описание**: 
Все активные API endpoints (67 файлов route.ts) мигрированы на централизованную систему `withErrorHandler`. Каждый endpoint может иметь несколько методов (GET, POST, PUT, DELETE), поэтому общее количество обработчиков больше (~71+).

**Endpoints с withErrorHandler** (67 файлов, ~71+ обработчиков):
- **Branches**: `branches/create`, `branches/[id]/delete`, `branches/[id]/update`, `branches/[id]/schedule`
- **Staff**: `staff/[id]/update`, `staff/[id]/delete`, `staff/create`, `staff/[id]/restore`, `staff/[id]/dismiss`, `staff/[id]/transfer`, `staff/create-from-user`, `staff/avatar/upload`, `staff/avatar/remove`, `staff/sync-roles`, `staff/update`
- **Staff Shifts**: `staff/shift/today` (deprecated), `staff/shift/open`, `staff/shift/close`, `staff/shift/items`, `staff/finance`
- **Services**: `services/create`, `services/[id]/update`, `services/[id]/delete`
- **Reviews**: `reviews/create`, `reviews/update`
- **Bookings**: `bookings/[id]/cancel`, `bookings/[id]/mark-attendance`
- **Auth**: `auth/sign-out`, `auth/telegram/login`, `auth/telegram/link`, `auth/mobile-exchange` (POST, GET), `auth/yandex/callback`, `auth/whatsapp/send-otp`, `auth/whatsapp/verify-otp`, `auth/whatsapp/create-session`
- **WhatsApp**: `whatsapp/get-phone-numbers`, `whatsapp/get-business-account`, `whatsapp/diagnose`, `whatsapp/test`, `whatsapp/verify-otp`, `whatsapp/send-otp`
- **Admin**: `admin/promotions/debug`, `admin/ratings/status`, `admin/health-check`, `admin/performance/stats`, `admin/initialize-ratings`
- **Dashboard**: `dashboard/staff/[id]/finance` (deprecated), `dashboard/staff/[id]/shift/open`, `dashboard/staff/[id]/finance/stats`, `dashboard/staff/finance/all`, `dashboard/staff-shifts/[id]/update-hours`, `dashboard/branches/[branchId]/promotions` (GET, POST), `dashboard/branches/[branchId]/promotions/[promotionId]` (PATCH, DELETE)
- **Other**: `notify` (POST), `notify/ping`, `quick-hold`, `quick-book-guest`, `users/search`, `profile/update`, `user/update-phone`, `metrics/frontend`, `webhooks/whatsapp` (GET, POST), `swagger.json`, `cron/close-shifts`, `cron/recalculate-ratings`, `cron/health-check-alerts`

**Решенные проблемы**:
- ✅ Стандартизированы форматы ответов об ошибках (используется `createErrorResponse`)
- ✅ Добавлено последовательное логирование всех ошибок (через `withErrorHandler`)
- ✅ Реализовано маскирование чувствительных данных в ошибках (частично)
- ✅ Упрощена отладка через централизованное логирование

**Бывшие риски** (устранены):
- ~~Сложность отладки~~ → Решено через централизованное логирование
- ~~Плохой UX~~ → Решено через стандартизированные сообщения об ошибках
- ~~Утечка чувствительных данных~~ → Решено через маскирование данных
- ~~Невозможность централизованного мониторинга~~ → Решено через `withErrorHandler`

**Статус**: ✅ **Миграция завершена!** Все активные API endpoints (67 файлов route.ts) мигрированы на `withErrorHandler`. Все обработчики используют стандартизированную обработку ошибок.

**Реализовано**:
- ✅ Стандартизирован формат ошибок (используется `createErrorResponse`)
- ✅ Добавлено автоматическое логирование всех ошибок (через `withErrorHandler`)
- ✅ Маскирование чувствительных данных в ошибках (частично реализовано)
- ⏳ Добавить трекинг ошибок в мониторинг (в планах)

**Документация:** `apps/web/src/lib/ERROR_HANDLING_GUIDE.md`

**Приоритет**: ✅ **ЗАВЕРШЕНО** (надежность и безопасность)

**Следующие шаги**:
- ⏳ Добавить трекинг ошибок в мониторинг (опционально)
- ⏳ Расширить маскирование чувствительных данных (опционально)

---

### 4. Высокий: Отсутствие проверки прав доступа в некоторых API endpoints

**Описание**: 
Не все API endpoints проверяют права доступа пользователя перед выполнением операций. Это может привести к несанкционированному доступу к данным.

**Проблемы**:
- Некоторые endpoints не проверяют принадлежность ресурса к бизнесу пользователя
- Отсутствие проверки ролей для административных операций
- Прямой доступ к данным без проверки прав

**Риски**:
- Утечка данных между бизнесами
- Несанкционированное изменение данных
- Нарушение конфиденциальности

**Реализовано**:
- ✅ Создана утилита `@/lib/authCheck` для централизованной проверки прав доступа
- ✅ Проведен аудит всех критических endpoints - все имеют проверки прав доступа
- ✅ Добавлена проверка прав доступа в `notify/route.ts` (пользователь должен быть клиентом бронирования или менеджером бизнеса)
- ✅ Проверены endpoints: `reviews/*` (проверка принадлежности отзыва пользователю), `promotions/*` (проверка принадлежности к бизнесу)
- ✅ `quick-hold` и `quick-book-guest` - публичные endpoints для создания бронирований (проверка авторизации и принадлежности филиала к бизнесу)

**Реализовано (тесты)**:
- ✅ Добавлены тесты для проверки прав доступа в `notify/route.ts` (клиент бронирования, менеджер бизнеса, запрет доступа)
- ✅ Тесты для `reviews/*` уже содержат проверки прав доступа (принадлежность отзыва пользователю)
- ✅ Тесты для `promotions/*` уже содержат проверки прав доступа (принадлежность филиала и акции к бизнесу)
- ✅ Тесты для `quick-hold` и `quick-book-guest` проверяют авторизацию и принадлежность филиала к бизнесу

**Документация**: `apps/web/src/lib/AUTH_CHECK_GUIDE.md`

**Приоритет**: ✅ **ЗАВЕРШЕНО** (безопасность)  
**Прогресс**: 100% (все критичные endpoints проверены и исправлены, документация создана, тесты добавлены для всех критичных endpoints)

---

### 5. Высокий: Недостаточная валидация входных данных

**Описание**: 
Не все API endpoints используют централизованную валидацию через Zod. Некоторые endpoints валидируют данные вручную или вообще не валидируют.

**Проблемы**:
- Разные подходы к валидации в разных endpoints
- Дублирование логики валидации
- Потенциальные уязвимости из-за отсутствия валидации
- Сложность поддержки валидации

**Примеры**:
- `services/create` - валидация вручную
- `staff/create` - валидация вручную
- Многие endpoints не используют `validateRequest` из `@/lib/validation/apiValidation`

**Риски**:
- SQL injection (хотя Supabase защищает, но лучше валидировать)
- XSS атаки через невалидированные данные
- Некорректные данные в БД
- Проблемы с безопасностью

**Рекомендации**:
- Создать централизованную систему валидации на основе Zod
- Мигрировать все endpoints на использование `validateRequest`
- Добавить валидацию для всех входных параметров (body, query, params)
- Добавить автоматические тесты для валидации
- Документировать схемы валидации

**Приоритет**: 🟠 **ВЫСОКИЙ** (безопасность и надежность)

---

### 6. Средний: Проблемы производительности фронтенда

**Описание**: 
Некоторые компоненты и страницы могут иметь проблемы с производительностью из-за отсутствия оптимизаций.

**Проблемы**:
- Некоторые списки без пагинации (могут загружать сотни элементов)
- Потенциальные N+1 запросы в некоторых местах
- Отсутствие мемоизации в некоторых тяжелых вычислениях
- Большие bundle размеры из-за отсутствия code splitting
- Отсутствие виртуализации для длинных списков

**Примеры**:
- `b/[slug]/view.tsx` - большой компонент с множеством данных
- Некоторые списки в dashboard без пагинации
- Потенциальные множественные запросы к БД

**Рекомендации**:
- ✅ Оптимизирован `ClientsList` - добавлена мемоизация вычислений
- ✅ Добавлена пагинация для `StaffListClient` (30 элементов на страницу)
- ✅ Добавлена мемоизация статистики и обработчиков в `StaffListClient`
- ⚠️ Добавить пагинацию для `BranchAdminsPanel` при большом количестве администраторов
- ⚠️ Применить code splitting для больших компонентов (`b/[slug]/view.tsx`, `dashboard/bookings/view.tsx`)
- ⚠️ Использовать виртуализацию для длинных списков (>100 элементов) - `react-window` или `react-virtuoso`
- ⚠️ Оптимизировать запросы к БД (использовать JOIN вместо множественных запросов где возможно)
- ⚠️ Провести аудит bundle size и применить tree shaking для неиспользуемых зависимостей

**Приоритет**: 🟡 **СРЕДНИЙ** (производительность и UX)  
**Прогресс**: ~40% (оптимизированы основные списки, требуется code splitting и виртуализация)

---

### 7. Средний: Потенциальные утечки памяти в React компонентах

**Описание**: 
Некоторые компоненты могут иметь проблемы с очисткой ресурсов (таймеры, подписки, AbortController).

**Проблемы**:
- `useEffect` без cleanup функций для таймеров
- AbortController без очистки при размонтировании
- Подписки на события без отписки
- Кэш без ограничения размера

**Примеры**:
- `useSlotsLoader` - есть cleanup для таймеров, но нужно проверить все случаи
- `useShiftItems` - есть cleanup для таймеров и AbortController
- Некоторые компоненты могут не очищать интервалы

**Рекомендации**:
- Провести аудит всех `useEffect` на наличие cleanup функций
- Убедиться, что все таймеры очищаются при размонтировании
- Убедиться, что все AbortController отменяются при размонтировании
- Добавить ограничения размера для кэшей
- Использовать ESLint правило `exhaustive-deps` для проверки зависимостей

**Приоритет**: 🟡 **СРЕДНИЙ** (производительность и стабильность)

---

### 8. Средний: Отсутствие интеграции ErrorBoundary с системой мониторинга

**Описание**: 
`ErrorBoundary` компонент не интегрирован с системой мониторинга ошибок (Sentry, LogRocket).

**Проблемы**:
- Ошибки React не логируются в систему мониторинга
- Нет трекинга ошибок на фронтенде
- Сложность отладки ошибок в продакшене

**Рекомендации**:
- Интегрировать `ErrorBoundary` с системой мониторинга
- Добавить логирование ошибок React в `logError`
- Добавить отправку ошибок в Sentry/LogRocket
- Добавить контекст ошибки (URL, пользователь, действия)

**Файл:** `apps/web/src/components/ErrorBoundary.tsx`

**Приоритет**: 🟡 **СРЕДНИЙ** (надежность и отладка)

---

### 9. Низкий: Отсутствие rate limiting для некоторых endpoints

**Описание**: 
Не все API endpoints защищены rate limiting. Это может привести к злоупотреблениям и DDoS атакам.

**Текущее состояние**:
- ✅ Rate limiting реализован для критичных endpoints (shift/open, shift/close, shift/items)
- ✅ Rate limiting для публичных endpoints (quick-book-guest, quick-hold)
- ✅ Rate limiting для auth endpoints (whatsapp/send-otp, whatsapp/verify-otp)
- ⚠️ Многие endpoints не имеют rate limiting

**Рекомендации**:
- Добавить rate limiting для всех публичных endpoints
- Добавить rate limiting для всех CRUD операций
- Настроить разные лимиты для разных типов операций
- Добавить мониторинг превышения лимитов

**Приоритет**: 🟢 **НИЗКИЙ** (безопасность, но Supabase RLS защищает от большинства атак)

---

## ✅ Выполненные задачи

### Завершено

1. ✅ **Рефакторинг больших компонентов**:
   - Созданы компоненты для формы бронирования: `StaffSelector`, `ServiceSelector`, `SlotPicker`, `BookingSummary`
   - Созданы компоненты для управления бронированиями: `BookingFilters`, `BookingActions`, `BookingsList`
   - Основные файлы рефакторены: `b/[slug]/view.tsx` (~1100 → ~900 строк), `dashboard/bookings/view.tsx` (~1384 → ~1200 строк)

2. ✅ **Устранение дублирования кода**:
   - Создан модуль `@/lib/supabaseHelpers.ts` для унификации создания Supabase клиентов
   - Создан модуль `@/lib/dbHelpers.ts` для унификации паттернов запросов к БД
   - Обновлены 21+ API endpoints для использования `supabaseHelpers`
   - Применен `dbHelpers` в 13 endpoints
   - Унифицировано форматирование дат в 4 компонентах
   - **Прогресс**: ~98%

3. ✅ **Улучшение типизации**:
   - Заменены все основные использования `any` на конкретные типы
   - Удалены `@ts-ignore` и `@ts-expect-error` (кроме обоснованных случаев)
   - Добавлены ESLint правила для запрета `any`
   - **Прогресс**: ~100%

4. ✅ **Миграция на валидацию UUID**:
   - Создана функция `getRouteParamUuid` в `@/lib/routeParams`
   - Мигрированы все основные route параметры на использование `getRouteParamUuid`
   - **Прогресс**: ~95%

5. ✅ **Документация API**:
   - Добавлена документация для 20+ API endpoints
   - Все endpoints включают описание, параметры, примеры ответов, коды ошибок
   - **Прогресс**: ~95%

6. ✅ **Мониторинг производительности фронтенда**:
   - Установлена библиотека `web-vitals`
   - Создана утилита `webVitals.ts` для трекинга Core Web Vitals
   - Создан API endpoint `/api/metrics/frontend` для приема метрик
   - Интегрирован компонент `PerformanceTracking` в корневой layout
   - **Прогресс**: ~90%

7. ✅ **Рефакторинг `/api/notify/route.ts`**:
   - Вынесена логика получения данных бронирования в `BookingDataService`
   - Созданы unit-тесты для `BookingDataService`
   - Улучшена модульность и тестируемость

---

## 🔮 Рекомендации на будущее

### Критический приоритет

1. **Завершить миграцию `console.log`**:
   - Мигрировать все оставшиеся 82 файла
   - Добавить автоматическую проверку в CI/CD с блокировкой деплоя
   - Настроить централизованное логирование

2. **Довести покрытие тестами до 70%+**:
   - Добавить тесты для всех критичных API endpoints
   - Покрыть тестами бизнес-логику (бронирования, уведомления, промоакции)
   - Настроить CI/CD с автоматическим запуском тестов
   - Установить минимальный порог покрытия

3. **Завершить миграцию обработки ошибок**:
   - Мигрировать все ~68 оставшихся API endpoints на `withErrorHandler`
   - Стандартизировать формат ошибок
   - Добавить централизованный мониторинг ошибок

### Высокий приоритет

4. **Централизованная валидация входных данных**:
   - Мигрировать все endpoints на использование `validateRequest` с Zod схемами
   - Добавить валидацию для всех входных параметров
   - Создать библиотеку общих схем валидации

5. **Аудит безопасности API**:
   - Проверить все endpoints на наличие проверки прав доступа
   - Добавить проверку прав во все endpoints
   - Провести penetration testing

6. **Оптимизация производительности**:
   - Добавить пагинацию для всех больших списков
   - Использовать виртуализацию для длинных списков
   - Оптимизировать запросы к БД
   - Применить code splitting

### Средний приоритет

7. **Интеграция ErrorBoundary с мониторингом**:
   - Интегрировать с Sentry/LogRocket
   - Добавить логирование ошибок React
   - Добавить контекст ошибки

8. **Аудит утечек памяти**:
   - Проверить все `useEffect` на наличие cleanup функций
   - Убедиться, что все таймеры очищаются
   - Добавить ограничения размера для кэшей

9. **Расширение rate limiting**:
   - Добавить rate limiting для всех публичных endpoints
   - Настроить разные лимиты для разных типов операций
   - Добавить мониторинг превышения лимитов

---

## 📊 Статистика проекта

- **Всего файлов TypeScript/TSX:** ~300+
- **Строк кода:** ~50,000+
- **API endpoints:** ~80+
- **Миграции БД:** 46
- **Тестов:** 24+ файлов (unit, integration, E2E)
- **Покрытие тестами:** ~30% (улучшено с < 25%)
- **Console.log:** ~312 использований в 82 файлах (осталось мигрировать)
- **Endpoints с withErrorHandler:** 12 из ~80+ (~15%)
- **Endpoints с валидацией Zod:** ~10 из ~80+ (~12%)
- **Большие компоненты (>1000 строк):** 0 файлов (улучшено с 2)

---

## ✅ Положительные моменты

1. **Хорошая структура проекта** - четкое разделение на apps и packages
2. **TypeScript** - используется везде, хорошая типизация
3. **Безопасность** - есть защита от использования service key в клиенте
4. **Логирование** - есть система безопасного логирования
5. **Обработка ошибок** - есть централизованная система (хотя не везде используется)
6. **RLS политики** - настроены и проверены
7. **Security headers** - настроены в middleware
8. **Rate limiting** - реализован для критичных endpoints
9. **Мониторинг производительности** - есть система отслеживания для API и фронтенда
10. **Тестирование** - добавлены unit, integration и E2E тесты (хотя покрытие низкое)
11. **Рефакторинг** - большие компоненты разделены на более мелкие
12. **Унификация** - созданы утилиты для устранения дублирования кода

---

## 🔗 Связанные документы

- `ISSUES_STAFF_FINANCE_PAGES.md` - Проблемы и оптимизации финансового модуля
- `PROJECT_DOCUMENTATION.md` - Общая техническая документация проекта
- `SYSTEM_FEATURES_DOCUMENTATION.md` - Документация функций системы
- `API_DOCUMENTATION.md` - Документация API endpoints
- `MONITORING_AND_ANALYTICS.md` - Документация по мониторингу и аналитике
- `MIGRATION_SUMMARY.md` - Резюме миграции console.log

---

*Документ создан: 2025-01-XX*  
*Последнее обновление: 2025-02-15*
