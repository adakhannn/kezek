# Проблемы и оптимизации проекта Kezek

## Статус документа
- **Дата создания**: 2025-01-XX
- **Последнее обновление**: 2025-02-15
- **Охват**: Весь проект (веб, мобильное приложение, API, база данных)
- **Статус**: ⚠️ Частично оптимизирован, есть активные проблемы

---

## 🔴 Актуальные проблемы

### 1. Критическое: Массовое использование `console.log` в production коде

**Описание**: 
В проекте осталось ~312 использований `console.log/warn/error` в 82 файлах, которые нужно мигрировать на безопасное логирование.

**Риски**:
- Утечка чувствительных данных в логах (токены, ключи, пароли)
- Проблемы с производительностью в продакшене
- Неконтролируемое логирование
- Нарушение безопасности данных

**Рекомендации**:
- Продолжить миграцию критичных компонентов и API routes
- Добавить автоматическую проверку в CI/CD (fail build при наличии console.log)
- Настроить централизованное логирование (Sentry, LogRocket)
- Добавить ESLint правило с ошибкой вместо предупреждения

**Документация:** 
- `apps/web/src/lib/CONSOLE_LOG_MIGRATION.md`
- `MIGRATION_SUMMARY.md`

**Приоритет**: 🔴 **КРИТИЧЕСКИЙ** (безопасность и производительность)

---

### 2. Критическое: Недостаточное покрытие тестами

**Описание**: 
Из ~80+ API endpoints только 19 файлов тестов. Многие критичные API endpoints и бизнес-логика не покрыты тестами. Нет тестов для уведомлений, бронирований, промоакций, большинства CRUD операций.

**Текущее покрытие**:
- ✅ Unit-тесты для `financeDomain` (6 файлов)
- ✅ Интеграционные тесты для API endpoints (22 файла):
  - ✅ Финансовые API (staff/finance, staff/shift/*)
  - ✅ Бронирования (bookings/cancel, bookings/mark-attendance)
  - ✅ Уведомления (notify) - **НОВОЕ**
  - ✅ CRUD операции (staff/create, branches/create, branches/delete, services/create) - **НОВОЕ**
  - ✅ Quick booking (quick-book-guest, quick-hold)
  - ✅ Cron задачи (close-shifts)
- ✅ E2E тесты для критичных сценариев (4 файла)
- ⚠️ **Покрытие ~30%** от общего количества endpoints (улучшено с < 25%)

**Риски**:
- Высокий риск регрессий при изменениях
- Сложность рефакторинга без уверенности
- Проблемы с качеством кода
- Невозможность безопасного деплоя

**Рекомендации**:
- ✅ Добавлены тесты для уведомлений (notify) и CRUD операций (branches, services)
- ⚠️ Добавить тесты для промоакций (promotions)
- ⚠️ Добавить тесты для auth endpoints (sign-in, sign-out, OAuth)
- ⚠️ Добавить тесты для остальных CRUD операций (staff/update, services/update, branches/update)
- Настроить CI/CD с автоматическим запуском тестов и блокировкой деплоя при падении
- Добавить E2E тесты для критичных пользовательских потоков
- Установить минимальный порог покрытия (например, 70%)

**Текущие тесты:** `apps/web/src/__tests__/api/`, `apps/web/src/__tests__/lib/`, `apps/web/e2e/`

**Приоритет**: 🔴 **КРИТИЧЕСКИЙ** (качество и надежность)

---

### 3. Высокий: Непоследовательная обработка ошибок в API

**Описание**: 
Из ~80+ API endpoints только 11 используют централизованную систему `withErrorHandler` (улучшено с 7). Остальные используют старую обработку ошибок с разными форматами ответов.

**Проблемы**:
- Разные форматы ответов об ошибках
- Непоследовательное логирование ошибок
- Потенциальная утечка чувствительных данных в ошибках
- Сложность отладки и мониторинга

**Риски**:
- Сложность отладки
- Плохой UX (непонятные сообщения об ошибках)
- Утечка чувствительных данных в ошибках
- Невозможность централизованного мониторинга ошибок

**Рекомендации**:
- ✅ Мигрированы критичные endpoints: `branches/create`, `branches/[id]/delete`, `staff/[id]/update`, `staff/[id]/delete`
- ⚠️ Мигрировать остальные API endpoints на `withErrorHandler`
- ✅ Стандартизирован формат ошибок (используется `createErrorResponse`)
- ✅ Добавлено автоматическое логирование всех ошибок (через `withErrorHandler`)
- ⚠️ Маскировать чувствительные данные в ошибках (частично реализовано)
- ⚠️ Добавить трекинг ошибок в мониторинг

**Документация:** `apps/web/src/lib/ERROR_HANDLING_GUIDE.md`

**Приоритет**: 🟠 **ВЫСОКИЙ** (надежность и безопасность)

---

### 4. Высокий: Большие монолитные компоненты

**Описание**: 
Несколько компонентов превышают 1000 строк кода, что усложняет поддержку и тестирование.

**Проблемные файлы**:
- `apps/web/src/app/b/[slug]/view.tsx` - **~1100 строк** - форма бронирования с множеством логики
- `apps/web/src/app/dashboard/bookings/view.tsx` - **~1384 строки** - управление бронированиями
- `apps/web/src/app/api/notify/route.ts` - **~149 строк** (частично рефакторен, но все еще большой)

**Проблемы**:
- Нарушение принципа единственной ответственности
- Смешивание UI, бизнес-логики и состояния
- Сложность тестирования
- Плохая переиспользуемость
- Большие bundle размеры

**Рекомендации**:
- ✅ Созданы и интегрированы компоненты для формы бронирования: `StaffSelector`, `ServiceSelector`, `SlotPicker`, `BookingSummary` (вынесены из `b/[slug]/view.tsx`)
- ✅ Созданы и интегрированы компоненты для управления бронированиями: `BookingFilters`, `BookingActions`, `BookingsList` (вынесены из `dashboard/bookings/view.tsx`)
- ✅ Бизнес-логика уже вынесена в хуки (`useBookingSteps`, `useBookingCreation`, `useSlotsLoader` и др.)
- ⚠️ Применить code splitting для больших компонентов (React.lazy) для оптимизации bundle size
- ✅ Основные файлы рефакторены: `b/[slug]/view.tsx` (~1100 → ~900 строк), `dashboard/bookings/view.tsx` (~1384 → ~1200 строк)

**Приоритет**: 🟠 **ВЫСОКИЙ** (поддерживаемость и производительность)

---

### 5. Высокий: Отсутствие проверки прав доступа в некоторых API endpoints

**Описание**: 
Не все API endpoints проверяют права доступа пользователя перед выполнением операций. Это может привести к несанкционированному доступу к данным.

**Проблемы**:
- Некоторые endpoints не проверяют принадлежность ресурса к бизнесу пользователя
- Отсутствие проверки ролей для административных операций
- Прямой доступ к данным без проверки прав

**Риски**:
- Утечка данных между бизнесами
- Несанкционированное изменение данных
- Нарушение конфиденциальности

**Рекомендации**:
- ✅ Создана утилита `@/lib/authCheck` для централизованной проверки прав доступа
- ✅ Исправлен `bookings/[id]/cancel` - теперь проверяет принадлежность к бизнесу для менеджеров
- ✅ Обновлены `staff/[id]/restore` и `staff/[id]/dismiss` - используют service client для обхода RLS
- ✅ Проведен аудит 20+ критических endpoints - большинство уже имеют проверки
- ✅ Проверены дополнительные endpoints: `dashboard/staff-shifts/[id]/update-hours`, `staff/avatar/*`, `profile/update`, `users/search`
- ⚠️ Провести дополнительный аудит оставшихся endpoints (notify, quick-hold, quick-book-guest)
- ⚠️ Добавить автоматические тесты для проверки прав доступа во всех endpoints
- ⚠️ Создать документацию по использованию утилит проверки прав доступа

**Приоритет**: 🟠 **ВЫСОКИЙ** (безопасность)  
**Прогресс**: ~75% (основные критические endpoints проверены и исправлены, требуется дополнительный аудит оставшихся endpoints)

---

### 6. Средний: Проблемы производительности фронтенда

**Описание**: 
Некоторые компоненты и страницы могут иметь проблемы с производительностью из-за отсутствия оптимизаций.

**Проблемы**:
- Некоторые списки без пагинации (могут загружать сотни элементов)
- Потенциальные N+1 запросы в некоторых местах
- Отсутствие мемоизации в некоторых тяжелых вычислениях
- Большие bundle размеры из-за отсутствия code splitting
- Отсутствие виртуализации для длинных списков

**Примеры**:
- `b/[slug]/view.tsx` - большой компонент с множеством данных
- Некоторые списки в dashboard без пагинации
- Потенциальные множественные запросы к БД

**Рекомендации**:
- ✅ Оптимизирован `ClientsList` - добавлена мемоизация вычислений `availableBookings` для предотвращения O(n²) сложности
- ✅ Добавлена пагинация для `StaffListClient` (30 элементов на страницу) для больших списков
- ✅ Добавлена мемоизация статистики и обработчиков в `StaffListClient`
- ⚠️ Добавить пагинацию для `BranchAdminsPanel` при большом количестве администраторов
- ⚠️ Применить code splitting для больших компонентов (`b/[slug]/view.tsx`, `dashboard/bookings/view.tsx`)
- ⚠️ Использовать виртуализацию для длинных списков (>100 элементов) - `react-window` или `react-virtuoso`
- ⚠️ Оптимизировать запросы к БД (использовать JOIN вместо множественных запросов где возможно)
- ⚠️ Провести аудит bundle size и применить tree shaking для неиспользуемых зависимостей

**Приоритет**: 🟡 **СРЕДНИЙ** (производительность и UX)  
**Прогресс**: ~40% (оптимизированы основные списки, требуется code splitting и виртуализация)

---

### 7. Средний: Монолитный API endpoint `/api/notify/route.ts`

**Описание**: 
Файл все еще содержит много логики получения данных и валидации, хотя был частично рефакторен.

**Текущее состояние**:
- ✅ Использует `NotificationOrchestrator` для отправки уведомлений
- ✅ Использует `withErrorHandler` для обработки ошибок
- ⚠️ Файл все еще содержит логику получения данных бронирования
- ⚠️ Сложно тестировать отдельные части

**Рекомендации**:
- Вынести получение данных бронирования в отдельный сервис `BookingDataService`
- Создать отдельные сервисы для каждого типа уведомления (Email, WhatsApp, Telegram)
- Добавить unit-тесты для отдельных компонентов
- Использовать паттерн Strategy для разных каналов уведомлений

**Файл:** `apps/web/src/app/api/notify/route.ts`

**Приоритет**: 🟡 **СРЕДНИЙ** (поддерживаемость)

---

### 8. Средний: Дублирование кода и логики

**Описание**: 
В проекте есть повторяющиеся паттерны и логика, которые можно вынести в переиспользуемые утилиты.

**Примеры дублирования**:
- Повторяющиеся паттерны запросов к БД
- Дублирование логики валидации в разных местах
- Повторяющиеся паттерны форматирования данных
- Дублирование логики работы с датами и временем

**Риски**:
- Сложность поддержки (нужно менять в нескольких местах)
- Риск рассинхронизации логики
- Увеличение размера кода

**Рекомендации**:
- ✅ Создан модуль `@/lib/supabaseHelpers.ts` для унификации создания Supabase клиентов
- ✅ Создан модуль `@/lib/dbHelpers.ts` для унификации паттернов запросов к БД
- ✅ Унифицировано форматирование времени в `staff/finance/utils.ts` (использует `@/lib/dateFormat`)
- ✅ Обновлены 21 API endpoints для использования `supabaseHelpers`:
  - `notify/route.ts`
  - `staff/[id]/restore/route.ts`
  - `staff/[id]/dismiss/route.ts`
  - `bookings/[id]/cancel/route.ts`
  - `profile/update/route.ts`
  - `auth/sign-out/route.ts`
  - `user/update-phone/route.ts`
  - `reviews/create/route.ts`
  - `reviews/update/route.ts`
  - `whatsapp/send-otp/route.ts`
  - `whatsapp/verify-otp/route.ts`
  - `quick-hold/route.ts` (частично)
  - `auth/telegram/link/route.ts`
  - `admin/ratings/status/route.ts`
  - `admin/health-check/route.ts`
  - `admin/initialize-ratings/route.ts`
  - `admin/promotions/debug/route.ts`
  - `auth/whatsapp/send-otp/route.ts`
  - `auth/whatsapp/verify-otp/route.ts`
  - `auth/telegram/login/route.ts`
  - `auth/whatsapp/create-session/route.ts`
- ✅ Применен `dbHelpers` в 13 endpoints для проверки принадлежности ресурсов:
  - `staff/[id]/update/route.ts`
  - `staff/[id]/delete/route.ts`
  - `branches/[id]/delete/route.ts`
  - `branches/[id]/update/route.ts`
  - `services/[id]/update/route.ts`
  - `services/[id]/delete/route.ts`
  - `bookings/[id]/mark-attendance/route.ts`
  - `staff/create-from-user/route.ts`
  - `staff/[id]/transfer/route.ts`
  - `dashboard/staff-shifts/[id]/update-hours/route.ts`
  - `staff/[id]/restore/route.ts`
  - `staff/[id]/dismiss/route.ts`
  - `staff/shift/items/route.ts`
- ✅ Проведен аудит компонентов на использование унифицированных функций валидации (большинство уже используют)
- ✅ Обновлены 4 компонента для использования унифицированных функций форматирования дат:
  - `staff/bookings/StaffBookingsView.tsx`
  - `admin/health-check/page.tsx`
  - `admin/ratings-status/page.tsx`
  - `dashboard/staff/[id]/finance/components/StaffFinanceStats.tsx`

**Приоритет**: 🟡 **СРЕДНИЙ** (поддерживаемость)  
**Прогресс**: ~98% (созданы основные утилиты, обновлено 21+ endpoints, применен dbHelpers в 13 endpoints, обновлено 4 компонента, требуется финальный аудит)

---

### 9. Низкий: Использование `any` и `@ts-ignore`

**Описание**: 
В проекте все еще есть использование `any` и `@ts-ignore`, что снижает преимущества TypeScript.

**Проблемы**:
- Потеря преимуществ TypeScript
- Потенциальные runtime ошибки
- Сложность рефакторинга
- Проблемы с автодополнением в IDE

**Риски**:
- Ошибки типизации в runtime
- Сложность поддержки кода
- Проблемы с автодополнением в IDE

**Рекомендации**:
- ✅ Заменить все `any` на конкретные типы
- ✅ Удалить `@ts-ignore` и исправить проблемы типизации
- ⚠️ Настроить строгий режим TypeScript (уже есть ESLint правило)
- ✅ Добавить ESLint правила для запрета `any` (уже добавлено)

**Выполнено**:
- ✅ Исправлено использование `any` в `api/staff/finance/route.ts` - заменено на `SupabaseClient`
- ✅ Исправлено использование `any` в `b/[slug]/utils.ts` - заменено на конкретные типы `ErrorWithDetails` и `ErrorWithCode`
- ✅ Исправлено использование `any` в `components/admin/users/UserRolesEditor.tsx` - заменено на тип `ApiResponse`
- ✅ Исправлено использование `any` в `__tests__/api/testHelpers.ts` - заменено на тип `MockSupabaseClient`
- ✅ Исправлено использование `any` в `api/staff/create/route.ts` - заменено на тип `RoleRow`
- ✅ Удалены `@ts-expect-error` в `api/auth/sign-out/route.ts` и `admin/api/users/[id]/suspend/route.ts` - заменены на проверку существования метода

**Приоритет**: 🟢 **НИЗКИЙ** (качество кода)  
**Прогресс**: ~100% (все основные использования `any` и `@ts-ignore` исправлены, тестовые файлы обновлены)

---

### 10. Низкий: Продолжение миграции на валидацию UUID

**Описание**: 
Не все endpoints используют безопасную валидацию UUID для route параметров. Остальные endpoints все еще используют прямую работу с параметрами.

**Текущее состояние**:
- ✅ Создана функция `getRouteParamUuid` в `@/lib/routeParams`
- ✅ Мигрированы критичные endpoints (6 файлов)
- ✅ Мигрированы остальные endpoints на использование `getRouteParamUuid`:
  - `staff/update/route.ts`
  - `dashboard/branches/[branchId]/promotions/route.ts`
  - `dashboard/branches/[branchId]/promotions/[promotionId]/route.ts`
  - `dashboard/staff/[id]/finance/route.ts`
  - `dashboard/staff/[id]/shift/open/route.ts`
  - `dashboard/staff/[id]/finance/stats/route.ts`

**Рекомендации**:
- ✅ Продолжить миграцию остальных endpoints на использование `getRouteParamUuid` (выполнено)
- ⚠️ Добавить валидацию для всех query параметров (опционально, для критичных параметров)

**Приоритет**: 🟢 **НИЗКИЙ** (Supabase защищает от SQL injection через параметризованные запросы)  
**Прогресс**: ~95% (все основные route параметры мигрированы на валидацию UUID)

---

### 11. Низкий: Отсутствие документации для некоторых API endpoints

**Описание**: 
Не все API endpoints имеют документацию в `API_DOCUMENTATION.md`. Это усложняет работу с API для разработчиков.

**Рекомендации**:
- ✅ Добавить документацию для всех API endpoints
- ✅ Включить примеры запросов и ответов
- ✅ Описать все возможные ошибки
- ✅ Добавить информацию о rate limiting

**Выполнено**:
- ✅ Добавлена документация для управления сотрудниками:
  - `POST /api/staff/[id]/update` - обновление сотрудника
  - `POST /api/staff/[id]/delete` - удаление сотрудника
  - `POST /api/staff/[id]/restore` - восстановление сотрудника
  - `POST /api/staff/[id]/dismiss` - увольнение сотрудника
  - `POST /api/staff/[id]/transfer` - перевод сотрудника
  - `POST /api/staff/create-from-user` - создание сотрудника из пользователя
- ✅ Добавлена документация для управления филиалами:
  - `POST /api/branches/create` - создание филиала
  - `POST /api/branches/[id]/delete` - удаление филиала
  - `POST/GET /api/branches/[id]/schedule` - расписание филиала
- ✅ Добавлена документация для управления услугами:
  - `POST /api/services/create` - создание услуги
  - `POST /api/services/[id]/update` - обновление услуги
  - `POST /api/services/[id]/delete` - удаление услуги
- ✅ Добавлена документация для отзывов:
  - `POST /api/reviews/create` - создание отзыва
  - `POST /api/reviews/update` - обновление отзыва
- ✅ Добавлена документация для дашборда:
  - `POST /api/dashboard/staff/[id]/shift/open` - открытие смены для сотрудника
  - `POST /api/dashboard/staff-shifts/[id]/update-hours` - корректировка часов
  - `GET /api/dashboard/staff/[id]/finance/stats` - статистика по сотруднику
- ✅ Добавлена документация для промоакций:
  - `PATCH /api/dashboard/branches/[branchId]/promotions/[promotionId]` - обновление промоакции
  - `DELETE /api/dashboard/branches/[branchId]/promotions/[promotionId]` - удаление промоакции
- ✅ Все endpoints включают:
  - Описание функциональности
  - Rate limiting информацию
  - Требования к авторизации
  - Параметры запроса с валидацией
  - Примеры ответов
  - Коды ошибок

**Приоритет**: 🟢 **НИЗКИЙ** (документация)  
**Прогресс**: ~95% (основные endpoints задокументированы, остались некоторые admin endpoints)

---

### 12. Низкий: Отсутствие мониторинга производительности на фронтенде

**Описание**: 
Нет системы мониторинга производительности фронтенда (Core Web Vitals, время загрузки страниц, метрики рендеринга).

**Рекомендации**:
- ✅ Добавить мониторинг Core Web Vitals
- ✅ Трекинг времени загрузки страниц
- ✅ Мониторинг метрик рендеринга
- ⚠️ Интеграция с инструментами мониторинга (Sentry, LogRocket) - опционально

**Выполнено**:
- ✅ Установлена библиотека `web-vitals` для Core Web Vitals
- ✅ Создана утилита `webVitals.ts` для трекинга:
  - Core Web Vitals (LCP, FID, CLS, FCP, TTFB, INP)
  - Время загрузки страниц (DOMContentLoaded, DOMInteractive, DOMComplete, First Paint, FCP, TTFB)
  - Метрики рендеринга (медленные рендеры >16ms)
- ✅ Создан API endpoint `/api/metrics/frontend` для приема метрик
- ✅ Создана SQL миграция для таблицы `frontend_metrics`
- ✅ Интегрирован компонент `PerformanceTracking` в корневой layout
- ✅ Используется `navigator.sendBeacon` для надежной доставки метрик
- ✅ Автоматическое определение рейтинга метрик (good/needs-improvement/poor)
- ✅ Логирование метрик в dev режиме

**Приоритет**: 🟢 **НИЗКИЙ** (мониторинг)  
**Прогресс**: ~90% (основной функционал реализован, опционально можно добавить интеграцию с Sentry/LogRocket)

---

## 🔮 Рекомендации на будущее

### Критический приоритет

1. **Завершить миграцию `console.log`**:
   - Мигрировать все оставшиеся 82 файла
   - Добавить автоматическую проверку в CI/CD с блокировкой деплоя
   - Настроить централизованное логирование

2. **Довести покрытие тестами до 70%+**:
   - Добавить тесты для всех критичных API endpoints
   - Покрыть тестами бизнес-логику (бронирования, уведомления, промоакции)
   - Настроить CI/CD с автоматическим запуском тестов
   - Установить минимальный порог покрытия

3. **Завершить миграцию обработки ошибок**:
   - Мигрировать все ~73 оставшихся API endpoints на `withErrorHandler`
   - Стандартизировать формат ошибок
   - Добавить централизованный мониторинг ошибок

### Высокий приоритет

4. **Рефакторинг больших компонентов**:
   - Разделить `b/[slug]/view.tsx` на более мелкие компоненты
   - Разделить `dashboard/bookings/view.tsx` на компоненты
   - Вынести бизнес-логику в хуки

5. **Аудит безопасности API**:
   - Проверить все endpoints на наличие проверки прав доступа
   - Добавить проверку прав во все endpoints
   - Провести penetration testing

6. **Оптимизация производительности**:
   - Добавить пагинацию для всех больших списков
   - Использовать виртуализацию для длинных списков
   - Оптимизировать запросы к БД
   - Применить code splitting

### Средний приоритет

7. **Рефакторинг `/api/notify/route.ts`**:
   - Вынести получение данных в отдельный сервис
   - Создать отдельные сервисы для каждого типа уведомления

8. **Устранение дублирования кода**:
   - Вынести повторяющиеся паттерны в утилиты
   - Унифицировать валидацию и форматирование

9. **Улучшение типизации**:
   - Заменить все `any` на конкретные типы
   - Удалить `@ts-ignore`
   - Настроить строгий режим TypeScript

### Низкий приоритет

10. **Дополнительные улучшения**:
    - Расширенная аналитика и графики для статистики
    - Алерты на аномалии в производительности
    - Мониторинг производительности фронтенда
    - Улучшение документации API

---

## 📊 Статистика проекта

- **Всего файлов TypeScript/TSX:** ~300+
- **Строк кода:** ~50,000+
- **API endpoints:** ~80+
- **Миграции БД:** 46
- **Тестов:** 22+ файлов (unit, integration, E2E)
- **Покрытие тестами:** ~30% (улучшено с < 25%)
- **Console.log:** ~312 использований в 82 файлах (осталось мигрировать)
- **Endpoints с withErrorHandler:** 7 из ~80+ (~9%)
- **Большие компоненты (>1000 строк):** 2 файла

---

## ✅ Положительные моменты

1. **Хорошая структура проекта** - четкое разделение на apps и packages
2. **TypeScript** - используется везде, хорошая типизация
3. **Безопасность** - есть защита от использования service key в клиенте
4. **Логирование** - есть система безопасного логирования
5. **Обработка ошибок** - есть централизованная система (хотя не везде используется)
6. **RLS политики** - настроены и проверены
7. **Security headers** - настроены в middleware
8. **Rate limiting** - реализован для API
9. **Мониторинг производительности** - есть система отслеживания для API
10. **Тестирование** - добавлены unit, integration и E2E тесты (хотя покрытие низкое)

---

## 🔗 Связанные документы

- `ISSUES_STAFF_FINANCE_PAGES.md` - Проблемы и оптимизации финансового модуля
- `PROJECT_DOCUMENTATION.md` - Общая техническая документация проекта
- `SYSTEM_FEATURES_DOCUMENTATION.md` - Документация функций системы
- `API_DOCUMENTATION.md` - Документация API endpoints
- `MONITORING_AND_ANALYTICS.md` - Документация по мониторингу и аналитике
- `MIGRATION_SUMMARY.md` - Резюме миграции console.log

---

*Документ создан: 2025-01-XX*  
*Последнее обновление: 2025-02-15*
