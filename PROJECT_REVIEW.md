# Проблемы и оптимизации проекта Kezek

## Статус документа
- **Дата создания**: 2025-01-XX
- **Последнее обновление**: 2026-02-22
- **Охват**: Весь проект (веб, мобильное приложение, API, база данных)
- **Статус**: ✅ Все пункты из `DEEP_ANALYSIS_ISSUES.md` закрыты; остаются только эволюционные улучшения (расширение E2E тестов, рост покрытия кода, дополнительный UX/a11y тюнинг)

**Проверка актуальности (раз в квартал):** Цифры в этом документе (покрытие тестами, количество API endpoints, файлов тестов, E2E-сценариев и т.п.) рекомендуется обновлять не реже **раз в квартал**. Аналогично — в [DEEP_ANALYSIS_ISSUES.md](DEEP_ANALYSIS_ISSUES.md). Источники: `pnpm -C apps/web test:coverage`, подсчёт route-файлов в `apps/web/src/app/api`, количество миграций в `supabase/migrations`.

---

## 🔴 Актуальные проблемы

### 1. ✅ ЗАВЕРШЕНО: Массовое использование `console.log` в production коде

**Описание**: 
Миграция на безопасное логирование завершена на 100%. Прямые `console.log/warn/error` остались только в инфраструктурных утилитах (`log.ts`, `logSafe.ts`) и dev‑инструментах (`useWhyDidYouRender`). Реализована жёсткая автоматическая защита от их повторного появления в продакшен‑коде.

**Бывшие риски** (устранены):
- ~~Потенциальная утечка чувствительных данных~~ → Решено через ESLint правила и автоматическое маскирование
- ~~Проблемы с производительностью~~ → Решено через контроль уровня логирования (debug только в dev)
- ~~Неконтролируемое логирование~~ → Решено через централизованную систему логирования

**Реализовано**:
- ✅ Включены строгие ESLint‑правила: `no-console` настроен как `error` для всего приложения (кроме явных исключений в лог‑утилитах)
- ✅ Добавлен CI‑пайплайн (`.github/workflows/ci.yml`), который на каждом `push`/PR запускает `pnpm -C apps/web lint` и `pnpm -C apps/web typecheck`; любое новое `console.*` в продакшен‑коде будет ломать сборку
- ✅ Создан документ с политикой логирования для команды: `apps/web/src/lib/LOGGING_POLICY.md`
- ✅ Документация включает правила, примеры использования, чеклист для разработчиков и инструкции по исправлению ошибок

**Документация:** 
- `apps/web/src/lib/LOGGING_POLICY.md` - **Политика логирования для команды** ✨
- `apps/web/src/lib/CONSOLE_LOG_MIGRATION.md` - Руководство по миграции
- `MIGRATION_SUMMARY.md` - Резюме миграции

**Приоритет**: ✅ **ЗАВЕРШЕНО** (безопасность и процессы логирования)  
**Прогресс**: 100% (техническая защита реализована, документация создана)

---

### 2. Критическое: Недостаточное покрытие тестами

**Описание**: 
Для ~80+ API endpoints написано уже ~70 файлов тестов (unit, integration, E2E), критичные API и основная бизнес‑логика покрыты. Тем не менее, общее покрытие кода по-прежнему невысокое (около 30%), часть менее критичных endpoint’ов и пользовательских сценариев остаются без тестов.

**Текущее покрытие (по endpoints)**:
- ✅ Unit-тесты для `financeDomain` (6 файлов)
- ✅ Интеграционные тесты для API endpoints (74 файла в `__tests__`):
  - ✅ Финансовые API (staff/finance, staff/shift/*, dashboard/staff/[id]/finance/stats, dashboard/staff/[id]/finance, dashboard/staff/finance/all, dashboard/finance/all)
  - ✅ Бронирования (bookings/cancel, bookings/mark-attendance)
  - ✅ Уведомления (notify)
  - ✅ CRUD операции (staff/create, staff/update, staff/delete, staff/dismiss, staff/restore, staff/transfer, staff/create-from-user, staff/sync-roles, branches/create, branches/update, branches/delete, branches/schedule, services/create, services/update, services/delete)
  - ✅ Профиль пользователя (profile/update, user/update-phone)
  - ✅ Аватары сотрудников (staff/avatar/upload, staff/avatar/remove)
  - ✅ Dashboard (staff-shifts/[id]/update-hours, staff/[id]/shift/open)
  - ✅ Поиск пользователей (users/search)
  - ✅ Quick booking (quick-book-guest, quick-hold)
  - ✅ Cron задачи (close-shifts)
  - ✅ **Auth endpoints (sign-out, telegram/login, telegram/link, whatsapp/send-otp, whatsapp/verify-otp, whatsapp/create-session, mobile-exchange, yandex/callback)** ✨
  - ✅ **Reviews endpoints (reviews/create, reviews/update)** ✨
  - ✅ **Promotions endpoints (branches/[branchId]/promotions - GET, POST, PATCH, DELETE)** ✨
  - ✅ **Admin endpoints (ratings/status, health-check, initialize-ratings, performance/stats, promotions/debug)** ✨
  - ✅ **WhatsApp endpoints (send-otp, verify-otp, get-phone-numbers, get-business-account, diagnose, test)** ✨
  - ✅ **Webhooks (whatsapp)** ✨
  - ✅ **Метрики (metrics/frontend)** ✨
- ✅ E2E тесты для критичных сценариев (7 спеков)
- ⚠️ Критичные и вспомогательные API покрыты тестами; суммарное покрытие кода — см. `pnpm -C apps/web test:coverage` (порог в CI: 40%)

**Отсутствуют тесты для**:
- Часть пользовательских сценариев UI всё ещё не покрыта E2E

**Реализовано**:
- ✅ Добавлены интеграционные тесты для промоакций, auth endpoints (sign-out, telegram, whatsapp, mobile-exchange, yandex/callback), CRUD‑операций staff/services/branches, уведомлений, метрик, webhooks, cron‑задач и вспомогательных endpoints (`swagger.json`, `notify/ping`)
- ✅ Настроен CI (`.github/workflows/ci.yml`), который на каждом `push`/PR автоматически:
  - запускает ESLint и typecheck для `apps/web`
  - запускает API‑тесты (`pnpm -C apps/web test`)
  - запускает E2E‑тесты (`pnpm -C apps/web test:e2e`)
  - при наличии `SUPABASE_TEST_DB_URL` выполняет критичные SQL‑тесты против тестовой БД

**Риски**:
- Регрессии в зонах без тестов (рефакторинг менее критичных функций/страниц остаётся рискованным)
- Невозможность ввести строгий порог покрытия без дополнительной работы

**Рекомендации (остаток работ)**:
- Расширить E2E‑набор для ключевых пользовательских потоков (создание/редактирование брони, акции, финансовые сценарии)
- Постепенно повышать покрытие и ввести минимальный **порог покрытия** в Jest (например, начать с 40–50% и увеличивать по мере роста тестов)

**Текущие тесты:** `apps/web/src/__tests__/api/`, `apps/web/src/__tests__/lib/`, `apps/web/e2e/`

**Прогресс по проблеме**: ✅ **Текущая цель выполнена на 100%** (все критичные и вспомогательные API endpoints имеют интеграционные тесты, настроен полный тестовый пайплайн в CI; дальнейшее увеличение процентного покрытия и E2E остаётся как эволюционное улучшение)

**Приоритет**: 🟠 **ВЫСОКИЙ** (дальнейший рост покрытия и E2E)

---

### 10. Средний: Неполная обработка входящих WhatsApp сообщений

**Описание**:  
Реализована базовая обработка входящих WhatsApp сообщений. Сообщения сохраняются в БД, привязываются к клиентам и бронированиям. Остаётся доработать автоматические ответы и обработку команд.

**Реализовано**:
- ✅ Создана таблица `whatsapp_messages` для хранения входящих сообщений (миграция `20260218000000_create_whatsapp_messages.sql`)
- ✅ Реализована обработка входящих текстовых сообщений в `webhooks/whatsapp/route.ts`
- ✅ Добавлена автоматическая привязка сообщений к клиентам (поиск по номеру телефона в `profiles`)
- ✅ Добавлена автоматическая привязка к активным бронированиям (поиск по `client_id` или `client_phone`)
- ✅ Сохранение полных данных от Meta в `raw_data` для отладки и будущей обработки
- ✅ Защита от дублирования сообщений (проверка `whatsapp_message_id`)
- ✅ Обновлены тесты для проверки обработки входящих сообщений

**Осталось**:
- ⚠️ Реализовать автоматические ответы на команды (отмена/подтверждение бронирования)
- ⚠️ Добавить обработку других типов сообщений (изображения, аудио, документы)
- ⚠️ Расширить тесты для проверки привязки к клиентам и бронированиям

**Документация:**
- Миграция: `supabase/migrations/20260218000000_create_whatsapp_messages.sql`
- Webhook: `apps/web/src/app/api/webhooks/whatsapp/route.ts`

**Приоритет**: 🟡 **СРЕДНИЙ** (функциональность и UX для WhatsApp‑канала)  
**Прогресс**: ~80% (базовая обработка реализована, остаются автоматические ответы и расширенная обработка)

---

## ✅ Выполненные задачи

### Завершено

1. ✅ **Рефакторинг больших компонентов**:
   - Созданы компоненты для формы бронирования: `StaffSelector`, `ServiceSelector`, `SlotPicker`, `BookingSummary`
   - Созданы компоненты для управления бронированиями: `BookingFilters`, `BookingActions`, `BookingsList`
   - Основные файлы рефакторены: `b/[slug]/view.tsx` (~1100 → ~900 строк), `dashboard/bookings/view.tsx` (~1384 → ~1200 строк)

2. ✅ **Устранение дублирования кода**:
   - Создан модуль `@/lib/supabaseHelpers.ts` для унификации создания Supabase клиентов
   - Создан модуль `@/lib/dbHelpers.ts` для унификации паттернов запросов к БД
   - Обновлены 21+ API endpoints для использования `supabaseHelpers`
   - Применен `dbHelpers` в 13 endpoints
   - Унифицировано форматирование дат в 4 компонентах
   - **Прогресс**: ~98%

3. ✅ **Улучшение типизации**:
   - Заменены все основные использования `any` на конкретные типы
   - Удалены `@ts-ignore` и `@ts-expect-error` (кроме обоснованных случаев)
   - Добавлены ESLint правила для запрета `any`
   - **Прогресс**: ~100%

4. ✅ **Миграция на валидацию UUID**:
   - Создана функция `getRouteParamUuid` в `@/lib/routeParams`
   - Мигрированы все основные route параметры на использование `getRouteParamUuid`
   - **Прогресс**: ~95%

5. ✅ **Документация API**:
   - Добавлена документация для 20+ API endpoints
   - Все endpoints включают описание, параметры, примеры ответов, коды ошибок
   - **Прогресс**: ~95%

6. ✅ **Мониторинг производительности фронтенда**:
   - Установлена библиотека `web-vitals`
   - Создана утилита `webVitals.ts` для трекинга Core Web Vitals
   - Создан API endpoint `/api/metrics/frontend` для приема метрик
   - Интегрирован компонент `PerformanceTracking` в корневой layout
   - **Прогресс**: ~90%

7. ✅ **Рефакторинг `/api/notify/route.ts`**:
   - Вынесена логика получения данных бронирования в `BookingDataService`
   - Созданы unit-тесты для `BookingDataService`
   - Улучшена модульность и тестируемость

8. ✅ **Централизованная обработка ошибок в API**:
   - Все активные API endpoints (71 файл route.ts) мигрированы на `withErrorHandler`
   - Стандартизированы форматы ответов об ошибках через `createErrorResponse`
   - Добавлено автоматическое логирование всех ошибок
   - Реализовано маскирование чувствительных данных в ошибках
   - **Документация:** `apps/web/src/lib/ERROR_HANDLING_GUIDE.md`
   - **Статус**: ✅ 100% завершено

9. ✅ **Проверка прав доступа в API endpoints**:
   - Создана утилита `@/lib/authCheck` для централизованной проверки прав доступа
   - Проведен аудит всех критических endpoints - все имеют проверки прав доступа
   - Добавлены тесты для проверки прав доступа во всех критичных endpoints
   - **Документация**: `apps/web/src/lib/AUTH_CHECK_GUIDE.md`
   - **Статус**: ✅ 100% завершено

10. ✅ **Централизованная валидация входных данных**:
   - Создана система валидации `@/lib/validation/apiValidation` с `validateRequest` и `validateQuery`
   - Мигрированы 27+ endpoints на использование Zod схем (24+ POST/PATCH, 3 GET с query валидацией)
   - Все схемы централизованы в `@/lib/validation/schemas.ts`
   - **Статус**: ✅ ~98% завершено (основные endpoints покрыты)

11. ✅ **Оптимизация производительности фронтенда**:
   - Добавлена пагинация для длинных списков (`StaffListClient`, `BranchAdminsPanel`)
   - Применен code splitting для больших компонентов (`next/dynamic`)
   - Добавлена виртуализация для длинных списков (`react-window`)
   - Оптимизированы запросы к БД (параллельная загрузка через `Promise.all`)
   - Оптимизирован bundle size (`optimizePackageImports`, tree shaking)
   - **Статус**: ✅ ~100% завершено (все основные оптимизации выполнены)

12. ✅ **Предотвращение утечек памяти в React компонентах**:
   - Проведен аудит всех `useEffect` на наличие cleanup функций
   - Исправлены все таймеры, AbortController и подписки без cleanup
   - Добавлены ограничения размера для кэшей с автоматической очисткой
   - **Статус**: ✅ ~100% завершено (все найденные проблемы исправлены)

13. ✅ **Интеграция ErrorBoundary с системой мониторинга**:
   - Добавлена утилита `errorMonitoring.ts` для централизованной отправки ошибок
   - Интеграция с Sentry/LogRocket (если инициализированы) и fallback на `logError`
   - Обновлен `ErrorBoundary.tsx` для автоматической отправки ошибок с контекстом
   - **Статус**: ✅ ~100% завершено (интеграция завершена, опционально можно добавить инициализацию внешних сервисов)

14. ✅ **Rate limiting для API endpoints**:
   - Создана утилита `rateLimit.ts` с поддержкой Upstash Redis и in-memory fallback
   - Внедрен rate limiting на все публичные и основные CRUD endpoints
   - Добавлено логирование превышений лимитов
   - **Статус**: ✅ ~100% завершено (все критичные endpoints защищены)

---

## 🔮 Рекомендации на будущее

### Высокий приоритет (эволюционное развитие)

1. **Постепенно расширять E2E тесты и покрытие кода**:
   - Добавлять E2E тесты для новых и наиболее важных пользовательских потоков
   - Повышать порог покрытия в Jest по мере роста тестовой базы (например, с 30% → 40–50% и далее)

### Средний приоритет

2. **Дальнейшее развитие WhatsApp‑канала**:
   - На базе уже реализованного webhook'а (`webhooks/whatsapp/route.ts`) и поддерживаемых команд (`отмена`, `подтвердить`, `помощь`) расширять сценарии автоответов
   - Расширять обработку медиа/статусов и сценарии интеграции с CRM и другими каналами связи

3. **Опциональные улучшения**:
   - Инициализировать и задокументировать конкретный провайдер мониторинга (Sentry/LogRocket), используя существующую обёртку `errorMonitoring.ts`
   - При необходимости донастроить точечные лимиты rate limiting (per-user/per-route) на базе `RateLimitConfigs`

---

## 📊 Статистика проекта

- **Всего файлов TypeScript/TSX:** ~300+
- **Строк кода:** ~50,000+
- **API endpoints (route-файлов):** 71
- **Миграции БД:** 60
- **Тестов:** 74 файла (unit, integration) + 7 E2E-спеков
- **Покрытие тестами:** см. `pnpm -C apps/web test:coverage`; порог в CI — 40%
- **Console.log:** ✅ Защита через ESLint и CI (прямых `console.*` в продакшен‑коде нет)
- **Endpoints с withErrorHandler:** 71 файлов (~100% актуальных endpoints)
- **Endpoints с валидацией Zod:** 27+ endpoints (все критичные и основные CRUD‑операции)
- **Endpoints с rate limiting:** Все публичные и основные CRUD endpoints защищены
- **Endpoints с проверкой прав доступа:** Все критичные endpoints проверены
- **Большие компоненты (>1000 строк):** 0 файлов (улучшено с 2)

---

## ✅ Положительные моменты

1. **Хорошая структура проекта** - четкое разделение на apps и packages
2. **TypeScript** - используется везде, хорошая типизация
3. **Безопасность** - есть защита от использования service key в клиенте
4. **Логирование** - есть система безопасного логирования
5. **Обработка ошибок** - ✅ централизованная система `withErrorHandler` используется во всех API endpoints
6. **RLS политики** - настроены и проверены
7. **Security headers** - настроены в middleware
8. **Rate limiting** - реализован для критичных endpoints
9. **Мониторинг производительности** - есть система отслеживания для API и фронтенда
10. **Тестирование** - добавлены unit, integration и E2E тесты (хотя покрытие низкое)
11. **Рефакторинг** - большие компоненты разделены на более мелкие
12. **Унификация** - созданы утилиты для устранения дублирования кода

---

## 🔗 Связанные документы

- `DEEP_ANALYSIS_ISSUES.md` - ✨ **Глубокий анализ проекта: все проблемы и оптимизации** (2026-02-18)
- `ISSUES_STAFF_FINANCE_PAGES.md` - Проблемы и оптимизации финансового модуля
- `PROJECT_DOCUMENTATION.md` - Общая техническая документация проекта
- `SYSTEM_FEATURES_DOCUMENTATION.md` - Документация функций системы
- `API_DOCUMENTATION.md` - Документация API endpoints
- `MONITORING_AND_ANALYTICS.md` - Документация по мониторингу и аналитике
- `MIGRATION_SUMMARY.md` - Резюме миграции console.log

---

*Документ создан: 2025-01-XX*
*Последнее обновление: 2026-02-22 (квартальная проверка цифр)*
