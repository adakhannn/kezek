# Обзор проекта Kezek

## Общая информация

**Kezek** - это система управления бронированиями для бизнесов (салоны красоты, студии и т.д.), построенная на Next.js 16, React 19, TypeScript и Supabase.

### Архитектура проекта

- **Monorepo** структура с использованием pnpm workspaces
- **apps/web** - Next.js веб-приложение (App Router)
- **apps/mobile** - React Native/Expo мобильное приложение
- **packages/core-domain** - общая доменная логика
- **supabase/** - миграции базы данных и SQL функции

### Технологический стек

- **Frontend**: Next.js 16, React 19, TypeScript, Tailwind CSS
- **Backend**: Next.js API Routes, Supabase (PostgreSQL)
- **Mobile**: React Native, Expo
- **База данных**: PostgreSQL (через Supabase)
- **Аутентификация**: Supabase Auth (email, phone, Telegram, WhatsApp)
- **Уведомления**: Resend (email), WhatsApp API, Telegram Bot API

---

## 🔴 ТОП-10 самых проблемных мест в проекте

### 1. Монолитный API endpoint `/api/notify/route.ts` (1017 строк)

**Проблема:**
- Огромный файл с множественной ответственностью
- Смешивает логику отправки email, WhatsApp, Telegram
- Сложно тестировать и поддерживать
- Много дублирования кода для разных типов получателей

**Риски:**
- Высокая сложность поддержки
- Сложность добавления новых каналов уведомлений
- Проблемы с тестированием отдельных частей

**Рекомендации:**
- Разбить на отдельные сервисы: `EmailNotificationService`, `WhatsAppNotificationService`, `TelegramNotificationService`
- Вынести общую логику в `NotificationOrchestrator`
- Создать отдельные endpoints для каждого типа уведомления
- Использовать паттерн Strategy для разных каналов

**Файл:** `apps/web/src/app/api/notify/route.ts`

---

### 2. Огромный React компонент `StaffFinanceView.tsx` (1599 строк)

**Проблема:**
- Нарушение принципа единственной ответственности
- Смешивает UI, бизнес-логику и состояние
- Сложно тестировать
- Плохая переиспользуемость

**Риски:**
- Высокая сложность поддержки
- Проблемы с производительностью (большой bundle)
- Сложность добавления новых функций

**Рекомендации:**
- Разбить на компоненты: `ShiftSummary`, `ClientsList`, `StatsView`, `ShiftControls`
- Вынести бизнес-логику в хуки: `useShiftManagement`, `useShiftStats`, `useShiftItems`
- Создать отдельные компоненты для форм редактирования
- Использовать композицию вместо монолитного компонента

**Файл:** `apps/web/src/app/staff/StaffFinanceView.tsx`

**Примечание:** Есть план рефакторинга для `view.tsx` (1629 строк), но `StaffFinanceView.tsx` еще не рефакторен.

---

### 3. Массовое использование `console.log` в production коде (339 вхождений в 82 файлах)

**Проблема:**
- Утечка чувствительных данных в логах
- Проблемы с производительностью в продакшене
- Неконтролируемое логирование
- Нет структурированного формата логов

**Риски:**
- Утечка токенов, ключей, паролей
- Перегрузка логов в продакшене
- Сложность мониторинга и отладки

**Рекомендации:**
- Заменить все `console.log` на безопасное логирование из `@/lib/log`
- Использовать `logDebug`, `logWarn`, `logError` с автоматическим маскированием
- Настроить централизованное логирование (Sentry, LogRocket)
- Добавить ESLint правило для запрета `console.log`

**Документация:** `apps/web/src/lib/CONSOLE_LOG_MIGRATION.md`

---

### 4. Недостаточное покрытие тестами (только 9 тестовых файлов)

**Проблема:**
- Критичные API endpoints не покрыты тестами
- Нет unit-тестов для бизнес-логики
- Нет интеграционных тестов для сложных сценариев
- Нет E2E тестов для критичных пользовательских потоков

**Риски:**
- Высокий риск регрессий при изменениях
- Сложность рефакторинга без уверенности
- Проблемы с качеством кода

**Рекомендации:**
- Добавить тесты для всех API endpoints
- Покрыть тестами критичную бизнес-логику (бронирования, финансы, уведомления)
- Настроить CI/CD с автоматическим запуском тестов
- Добавить E2E тесты для критичных сценариев (Playwright уже настроен)

**Текущие тесты:** `apps/web/src/__tests__/api/`

---

### 5. Использование `any` и `@ts-ignore` (найдено несколько использований)

**Проблема:**
- Потеря преимуществ TypeScript
- Потенциальные runtime ошибки
- Сложность рефакторинга

**Риски:**
- Ошибки типизации в runtime
- Сложность поддержки кода
- Проблемы с автодополнением в IDE

**Рекомендации:**
- Заменить все `any` на конкретные типы
- Удалить `@ts-ignore` и исправить проблемы типизации
- Настроить строгий режим TypeScript
- Добавить ESLint правила для запрета `any`

---

### 6. Потенциальные проблемы с производительностью (N+1 запросы, большие компоненты)

**Проблема:**
- Большие компоненты загружают много данных сразу
- Потенциальные N+1 запросы в некоторых местах
- Отсутствие пагинации в некоторых списках
- Большие bundle размеры

**Риски:**
- Медленная загрузка страниц
- Высокая нагрузка на базу данных
- Плохой UX для пользователей

**Рекомендации:**
- Добавить пагинацию для больших списков
- Использовать React Query для кеширования и оптимизации запросов
- Оптимизировать запросы к БД (использовать JOIN вместо множественных запросов)
- Разделить код (code splitting) для больших компонентов
- Использовать виртуализацию для длинных списков

**Примеры проблемных мест:**
- `apps/web/src/app/dashboard/staff/[id]/page.tsx` - множественные запросы ✅ **ИСПРАВЛЕНО**: Объединены 3 запроса в 1 с JOIN
- `apps/web/src/app/b/[slug]/view.tsx` - большой компонент с множеством данных
- `apps/web/src/app/dashboard/bookings/view.tsx` - отсутствие пагинации ✅ **ИСПРАВЛЕНО**: Добавлена пагинация (30 элементов на страницу)
- `apps/web/src/app/b/[slug]/page.tsx` - промоакции без лимита ✅ **ИСПРАВЛЕНО**: Добавлен лимит 50 промоакций

**Выполненные оптимизации:**
1. ✅ `dashboard/staff/[id]/page.tsx`: Объединены запросы bookings, reviews и services в один запрос с JOIN, что уменьшило количество запросов к БД с 3 до 1
2. ✅ `b/[slug]/page.tsx`: Добавлен лимит 50 для промоакций, чтобы избежать загрузки слишком большого количества данных
3. ✅ `dashboard/bookings/view.tsx`: Добавлена пагинация с 30 элементами на страницу, включая UI для навигации и отображение общего количества записей

---

### 7. Непоследовательная обработка ошибок

**Проблема:**
- Не все API endpoints используют централизованную обработку ошибок
- Разные форматы ответов об ошибках
- Некоторые места не логируют ошибки

**Риски:**
- Сложность отладки
- Плохой UX (непонятные сообщения об ошибках)
- Утечка чувствительных данных в ошибках

**Рекомендации:**
- Использовать `withErrorHandler` или `createErrorResponse` везде
- Стандартизировать формат ошибок
- Добавить автоматическое логирование всех ошибок
- Маскировать чувствительные данные в ошибках

**Документация:** `apps/web/src/lib/ERROR_HANDLING_GUIDE.md`

**Выполненные миграции:**
1. ✅ `api/admin/promotions/debug/route.ts`: Мигрирован на `withErrorHandler`, все ошибки используют `createErrorResponse`
2. ✅ `api/users/search/route.tsx`: Мигрирован на `withErrorHandler`, стандартизированы ответы об ошибках
3. ✅ `api/auth/sign-out/route.ts`: Мигрирован на `withErrorHandler`, сохранено логирование
4. ✅ `api/bookings/[id]/mark-attendance/route.ts`: Мигрирован на `withErrorHandler`, все ошибки используют стандартный формат

**Осталось:**
- Проверить и мигрировать остальные API endpoints (приоритет: критичные endpoints для бизнес-логики)

---

### 8. Дублирование кода и логики

**Проблема:**
- Повторяющаяся логика валидации телефонов
- Дублирование кода для работы с датами
- Повторяющиеся паттерны запросов к БД

**Риски:**
- Сложность поддержки (нужно менять в нескольких местах)
- Риск рассинхронизации логики
- Увеличение размера кода

**Рекомендации:**
- Вынести общую логику в утилиты
- Создать переиспользуемые хуки
- Использовать композицию вместо дублирования
- Провести рефакторинг для устранения дублирования

**Примеры:**
- Валидация телефонов в разных местах
- Форматирование дат в разных компонентах
- Логика работы с бронированиями

**Выполненные улучшения:**
1. ✅ Унифицирована валидация телефонов: удалена дублирующаяся функция `isE164Phone` из `api/admin/users/create/route.ts`, используется общая функция `isE164` из `@/lib/validation`
2. ✅ Созданы общие утилиты для форматирования дат (`apps/web/src/lib/dateFormat.ts`):
   - `formatDate` - форматирование даты в формате DD.MM.YYYY
   - `formatDateBrowser` - форматирование с учетом локали браузера
   - `formatMonthYear` - форматирование месяца и года
   - `formatDateTime` - форматирование даты и времени
   - `formatTime` - форматирование только времени
3. ✅ Рефакторинг компонентов: `StaffFinanceStats.tsx` теперь использует общие утилиты вместо дублирующегося кода

**Осталось:**
- Вынести повторяющиеся паттерны запросов к БД в переиспользуемые утилиты (приоритет: низкий, так как большинство запросов специфичны)

---

### 9. Проблемы с безопасностью (потенциальные)

**Проблема:**
- Использование service role key требует осторожности
- Некоторые API endpoints могут не проверять права доступа
- Потенциальные SQL injection (хотя Supabase защищает)

**Риски:**
- Утечка данных
- Несанкционированный доступ
- Проблемы с RLS политиками

**Рекомендации:**
- Регулярно проверять использование service key (есть скрипты)
- Добавить проверки прав доступа во все API endpoints
- Провести аудит RLS политик (частично сделано)
- Использовать параметризованные запросы везде

**Документация:**
- `apps/web/src/lib/SECURITY.md`
- `RLS_AUDIT_REPORT.md`
- Скрипты проверки: `scripts/check-service-key-security.*`

**Выполненные улучшения безопасности:**
1. ✅ Добавлена валидация UUID для route параметров:
   - Создана функция `getRouteParamUuid` в `@/lib/routeParams` для безопасного извлечения и валидации UUID из route параметров
   - Обновлены критичные endpoints для использования валидации UUID:
     - `api/bookings/[id]/mark-attendance/route.ts`
     - `api/bookings/[id]/cancel/route.ts`
     - `api/staff/[id]/update/route.ts`
     - `api/staff/[id]/delete/route.ts`
     - `api/branches/[id]/delete/route.ts`
     - `api/branches/[id]/schedule/route.ts`

2. ✅ Добавлена валидация query параметров:
   - `api/admin/promotions/debug/route.ts`: валидация UUID для clientId, branchId, bizId
   - `api/users/search/route.tsx`: санитизация и ограничение длины поискового запроса, валидация page и perPage

3. ✅ Проверка использования service client:
   - Все endpoints, использующие service client, проверяют права доступа через `getBizContextForManagers` или проверку super_admin
   - `api/auth/sign-out/route.ts`: использует service client только для инвалидации токенов текущего пользователя (безопасно)

**Осталось:**
- Продолжить миграцию остальных endpoints на использование `getRouteParamUuid` (приоритет: средний, так как Supabase защищает от SQL injection через параметризованные запросы)

---

### 10. Отсутствие документации для сложных частей

**Проблема:**
- Сложная бизнес-логика не документирована
- Нет описания архитектурных решений
- Отсутствует документация API для разработчиков

**Риски:**
- Сложность онбординга новых разработчиков
- Потеря знаний при уходе разработчиков
- Сложность поддержки

**Рекомендации:**
- Добавить JSDoc комментарии для сложных функций
- Создать архитектурную документацию
- Документировать бизнес-логику (финансы, бронирования)
- Обновить README с описанием структуры проекта

**Существующая документация:**
- `PROJECT_DOCUMENTATION.md` - общая структура
- `SYSTEM_FEATURES_DOCUMENTATION.md` - функции системы
- Различные MD файлы в `apps/web/src/lib/`

---

## 📊 Статистика проекта

- **Всего файлов TypeScript/TSX:** ~300+
- **Строк кода:** ~50,000+
- **API endpoints:** ~80+
- **Миграции БД:** 46
- **Тестов:** 9 файлов
- **Console.log:** 339 вхождений в 82 файлах

---

## ✅ Положительные моменты

1. **Хорошая структура проекта** - четкое разделение на apps и packages
2. **TypeScript** - используется везде, хорошая типизация
3. **Безопасность** - есть защита от использования service key в клиенте
4. **Логирование** - есть система безопасного логирования (хотя не везде используется)
5. **Обработка ошибок** - есть централизованная система (хотя не везде используется)
6. **RLS политики** - настроены и проверены
7. **Security headers** - настроены в middleware
8. **Rate limiting** - реализован для API
9. **Мониторинг производительности** - есть система отслеживания

---

## 🎯 Приоритеты для улучшения

### Высокий приоритет (критично)
1. ✅ Рефакторинг `/api/notify/route.ts` (1017 строк)
2. ✅ Рефакторинг `StaffFinanceView.tsx` (1599 строк)
3. ✅ Замена всех `console.log` на безопасное логирование
4. ✅ Добавление тестов для критичных API endpoints

### Средний приоритет (важно)
5. ✅ Устранение использования `any` и `@ts-ignore`
6. ✅ Оптимизация производительности (N+1, пагинация)
7. ✅ Стандартизация обработки ошибок
8. ✅ Устранение дублирования кода

### Низкий приоритет (желательно)
9. ✅ Улучшение документации
10. ✅ Дополнительные проверки безопасности

---

## 📝 Рекомендации по процессу разработки

1. **Code Review** - обязательный для всех изменений
2. **Тесты** - писать тесты для нового функционала
3. **Рефакторинг** - выделять время на рефакторинг больших файлов
4. **Документация** - документировать сложные решения
5. **Мониторинг** - отслеживать производительность и ошибки

---

*Документ создан: 2025-01-XX*
*Последнее обновление: 2025-01-XX*

