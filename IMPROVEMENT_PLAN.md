## План проблемных зон Kezek

Этот список — только **актуальные проблемы и долги**, без уже решённых задач.

### 1. Стабильность денег и смен

1. **Потенциальные race conditions при открытии/закрытии смен**
   - При одновременном открытии/закрытии смены несколькими запросами могут возникнуть конфликты.
   - Нужно добавить транзакции или блокировки для критичных операций (открытие/закрытие смены, обновление финансов).
   - Проверить логику переоткрытия закрытых смен — может ли это привести к потере данных.

2. **Оптимизация запросов к БД в финансовых отчётах**
   - В `/api/dashboard/finance/all` и `/api/dashboard/staff/[id]/finance/stats` выполняются множественные запросы.
   - Рассмотреть использование агрегатных функций SQL или материализованных представлений для ускорения.
   - Добавить кэширование для часто запрашиваемых данных (например, статистика за сегодня).

---

### 2. Бронирование и UX

3. **Дальнейший рефакторинг `BookingForm`**
   - Компонент всё ещё содержит 46+ хуков (`useEffect`, `useState`, `useMemo`, `useCallback`).
   - Логика временных переводов, фильтрации услуг и загрузки слотов переплетена.
   - Задача: выделить отдельные хуки для:
     - управления временными переводами (`useTemporaryTransfers`);
     - фильтрации услуг (`useServicesFilter`);
     - загрузки слотов (`useSlotsLoader`);
     - управления шагами (уже частично сделано, но можно улучшить).

4. **Производительность загрузки слотов**
   - При каждом изменении `serviceId`, `staffId`, `dayStr` выполняется RPC-запрос.
   - Добавить debounce для частых изменений.
   - Рассмотреть кэширование слотов на клиенте (например, для одного дня/мастера/услуги).

---

### 3. Качество кода и технический долг

5. **Унификация логирования**
   - В коде найдено 481+ использований `console.log/error/warn` (включая 20+ файлов в `apps/web/src/app/api`).
   - Заменить все на `logDebug`, `logWarn`, `logError` из `@/lib/log`.
   - Это улучшит диагностику в продакшене и позволит контролировать уровень логирования.

6. **Обработка ошибок в API routes**
   - Некоторые API routes не имеют полной обработки ошибок (например, `whatsapp/*`, `auth/telegram/*`).
   - Стандартизировать формат ответов об ошибках.
   - Добавить централизованный error handler middleware.

7. **Валидация данных на клиенте**
   - Не все формы имеют клиентскую валидацию перед отправкой на сервер.
   - Добавить валидацию для:
     - форм создания/редактирования услуг;
     - форм создания/редактирования сотрудников;
     - форм бронирования (особенно для гостевых броней).

---

### 4. Безопасность и производительность

8. **Проверка и усиление RLS политик**
   - Провести аудит всех RLS политик на предмет утечек данных.
   - Убедиться, что владельцы бизнесов не могут видеть данные других бизнесов.
   - Проверить, что сотрудники не могут изменять данные других сотрудников.

9. ~~**Rate limiting для API endpoints**~~ ✅ **Выполнено**
   - ✅ Реализована утилита rate limiting (`apps/web/src/lib/rateLimit.ts`)
   - ✅ Применен rate limiting к публичным endpoints (`/api/quick-book-guest`, `/api/quick-hold`)
   - ✅ Применен rate limiting к критичным операциям (`/api/staff/shift/open`, `/api/staff/shift/close`)
   - ✅ Применен rate limiting к обычным операциям (`/api/staff/shift/items`, `/api/bookings/[id]/mark-attendance`)
   - ✅ Применен rate limiting к аутентификационным endpoints (`/api/whatsapp/send-otp`, `/api/whatsapp/verify-otp`, `/api/auth/telegram/login`, `/api/auth/telegram/link`)
   - 📝 Создана документация `RATE_LIMITING.md`
   - 💡 **Рекомендация для продакшена:** Использовать Redis/Upstash для распределенного rate limiting

10. ~~**Мониторинг производительности**~~ ✅ **Выполнено**
    - ✅ Создана утилита мониторинга производительности (`apps/web/src/lib/performance.ts`)
    - ✅ Добавлены метрики времени выполнения для критичных операций:
      - ✅ загрузка слотов (`get_free_slots_service_day_v2`);
      - ✅ закрытие смены;
      - ✅ применение промо;
      - ✅ пересчёт рейтингов.
    - ✅ Настроены пороговые значения и алерты при деградации производительности
    - ✅ Создан API endpoint `/api/admin/performance/stats` для просмотра метрик
    - ✅ Создана админ-страница `/admin/performance` для визуализации метрик
    - 💡 **Рекомендация для продакшена:** Интегрировать с внешним сервисом мониторинга (Datadog, New Relic, CloudWatch)

---

### 5. Тестирование

11. ~~**Тесты для критичных API endpoints**~~ ✅ **Выполнено**
    - ✅ Настроен Jest для тестирования Next.js API routes
    - ✅ Созданы unit-тесты для критичных endpoints:
      - ✅ `/api/staff/shift/close` (закрытие смены с гарантией);
      - ✅ `/api/bookings/[id]/mark-attendance` (применение промо);
      - ✅ `/api/dashboard/finance/all` (агрегация финансов);
      - ✅ `/api/cron/close-shifts` (автоматическое закрытие смен).
    - ✅ Добавлены тесты для edge cases (нулевые суммы, отрицательные значения, некорректные даты, отсутствие данных)
    - ✅ Создана документация `API_TESTING.md`
    - 💡 **Рекомендация:** Добавить интеграционные тесты с test database и E2E тесты для критичных сценариев

12. ~~**E2E тесты для критичных пользовательских сценариев**~~ ✅ **Выполнено**
    - ✅ Настроен Playwright для E2E тестирования
    - ✅ Создан тест для полного цикла бронирования (выбор филиала → мастера → услуги → времени → подтверждение)
    - ✅ Создан тест для открытия смены → добавления клиентов → закрытия смены
    - ✅ Создан тест для применения промо при бронировании
    - ✅ Интегрированы E2E тесты в CI pipeline
    - ✅ Создана документация `E2E_TESTING.md`
    - 💡 **Рекомендация:** Добавить `data-testid` атрибуты в UI компоненты для стабильности тестов

---

### 6. Мобильное приложение

13. ✅ **Синхронизация веб и мобильной версий** (Выполнено)
    - **Контекст:** Мобильное приложение предназначено только для клиентов и сотрудников (не для владельцев бизнеса).
    - **Бронирование:**
      - Мобильное приложение использует `/api/quick-hold` (для авторизованных пользователей).
      - Убедиться, что логика бронирования синхронизирована с веб-версией.
      - Проверить обработку ошибок и валидацию данных в мобильном приложении.
      - Убедиться, что временные слоты загружаются одинаково (используется ли `get_free_slots_service_day_v2`).
    - **Промоакции:**
      - Промо применяются автоматически при отметке посещения (на сервере), но нужно проверить:
        - Отображаются ли активные промоакции на странице бизнеса в мобильном приложении.
        - Показывается ли информация о примененной промо после создания бронирования.
        - Корректно ли отображается итоговая сумма со скидкой (если промо применяется сразу).
      - Убедиться, что промо применяются одинаково в веб и мобильной версиях (используется ли один и тот же RPC `apply_promotion`).
    - **Рейтинги:**
      - Проверить, что рейтинги бизнесов, филиалов и мастеров отображаются в мобильном приложении:
        - На главной странице (список бизнесов).
        - На странице бронирования (рейтинг мастера, филиала).
        - В личном кабинете клиента (если есть история с рейтингами).
      - Убедиться, что рейтинги синхронизированы с веб-версией (используются ли те же SQL функции для расчета).
    - **Функционал для сотрудников:**
      - В мобильном приложении есть `StaffScreen` с предстоящими записями, но отсутствует:
        - Управление сменами (открытие/закрытие смены).
        - Добавление клиентов в открытую смену.
        - Просмотр финансовой статистики.
      - Рассмотреть добавление функционала управления сменами в мобильное приложение для сотрудников.
    - **API endpoints:**
      - Убедиться, что мобильное приложение использует те же API endpoints, что и веб-версия.
      - Проверить совместимость версий API между веб и мобильным приложением.

---

### 7. Технический долг

15. ✅ **Документация API** (Выполнено)
    - Создана централизованная документация в файле `API_DOCUMENTATION.md`.
    - Документированы все основные API endpoints с описанием параметров, ответов и примеров использования.
    - Добавлена информация о rate limiting, авторизации, обработке ошибок и временных зонах.

---

### 8. UX улучшения

16. ✅ **Улучшение обратной связи при длительных операциях** (Выполнено)
    - Добавлена система Toast-уведомлений для отображения успешных операций и ошибок.
    - Добавлены компоненты ProgressBar и LoadingOverlay для индикации прогресса.
    - При закрытии смены отображается прогресс-бар с этапами обработки.
    - Улучшены сообщения об ошибках: теперь показываются понятные сообщения вместо "Internal Server Error".
    - Добавлены индикаторы загрузки для начальной загрузки данных.
    - Все alert() заменены на toast-уведомления.

17. ✅ **Оптимизация мобильного UX** (Выполнено)
    - Оптимизированы размеры кнопок: минимальный размер 44x44px на мобильных (рекомендации Apple/Google).
    - Оптимизированы поля ввода: увеличен размер шрифта до 16px на мобильных для предотвращения зума на iOS.
    - Улучшены календари и селекторы для touch-устройств: увеличены размеры кнопок и элементов управления.
    - Добавлен CSS `touch-manipulation` для улучшения производительности на touch-устройствах.
    - Оптимизированы все формы в booking flow для удобного использования на мобильных.
    - Добавлены адаптивные размеры для всех интерактивных элементов (кнопки, поля ввода, календари).

---

## Приоритеты

**Высокий приоритет:**
- Пункты 1, 3, 5, 8, 9 (стабильность, безопасность, качество кода)

**Средний приоритет:**
- Пункты 2, 4, 6, 7, 10, 11 (производительность, тестирование, UX)

**Низкий приоритет:**
- Пункты 12, 13, 14, 15, 16, 17 (технический долг, документация, улучшения UX)
