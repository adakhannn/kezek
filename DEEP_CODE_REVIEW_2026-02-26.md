## Глубокий обзор кода Kezek — 26.02.2026

Документ фиксирует результаты глубокого просмотра **кода** (без опоры на документацию) и предлагает конкретные задачи по улучшению.

Статусы:
- [ ] `todo` — задача запланирована
- [ ] `in-progress` — в работе
- [x] `done` — выполнено

Приоритеты:
- 🔴 критичный
- 🟠 высокий
- 🟡 средний
- 🟢 низкий

---

## 1. Dashboard bookings: god‑component и плотная связка с Supabase

**Файл(ы):**
- `apps/web/src/app/dashboard/bookings/view.tsx`

**Наблюдение:**
- Один очень большой компонент (1500+ строк), который совмещает:
  - несколько флоу (календарь, список с фильтрами/пресетами, QuickDesk),
  - прямые Supabase‑вызовы (`confirm_booking`, `cancel_booking`, `create_internal_booking`, `get_free_slots_service_day_v2`),
  - бизнес‑правила и сайд‑эффекты (уведомления, трекинг, статистика).
- Это создаёт сильную связность UI ↔ инфра ↔ домен и делает изменения рискованными.

**Задачи:**
- [x] 🟠 **CODE-BOOKINGS-1**: Разбить `AdminBookingsView` на подкомпоненты и хуки
  - Вынести состояние фильтров/пресетов/вкладок в хук вида `useDashboardBookingsFilters`.
  - Вынести QuickDesk в отдельный компонент/папку (например, `QuickDeskPanel` + свой хук состояния).
  - Оставить в `view.tsx` только сборку layout и подключение подкомпонентов.

- [x] 🟠 **CODE-BOOKINGS-2**: Вынести Supabase‑логику бронирований в сервисный слой
  - Создать модуль в `apps/web/src/lib/bookingDashboardService.ts` (или аналогичный), который:
    - инкапсулирует вызовы RPC/таблиц (`confirm_booking`, `cancel_booking`, `create_internal_booking`, `get_free_slots_service_day_v2`),
    - возвращает типизированные DTO, не зависящие от Supabase‑структур.
  - Заменить прямые обращения к Supabase в `view.tsx` вызовами этого сервиса.

**Предложение по решению (кратко):**
- Ввести границу: `AdminBookingsView` → (хуки/подкомпоненты) → `bookingDashboardService` → Supabase/доменные use‑cases.
- Делать рефакторинг поэтапно: сначала вынос состояния/хуков без изменения поведения, затем миграция RPC‑вызовов в сервис.

---

## 2. Дублирование RPC‑логики слотов и внутренних бронирований (web + mobile)

**Файл(ы):**
- `apps/web/src/app/dashboard/bookings/view.tsx`
- `apps/web/src/app/dashboard/staff/[id]/slots/Client.tsx`
- `apps/mobile/src/screens/booking/BookingStep5Time.tsx`

**Наблюдение:**
- Одинаковые RPC‑вызовы (`get_free_slots_service_day_v2`, `create_internal_booking`) используются в разных местах UI.
- Повторяется логика фильтрации слотов (только будущее, по сотруднику/филиалу и т.п.).
- Любое изменение схемы RPC требует ручной правки сразу в нескольких компонентах (в том числе в другом приложении).

**Задачи:**
- [x] 🟠 **CODE-SLOTS-1**: Ввести единый сервис для работы со слотами
  - Создать модуль, например:
    - `packages/shared-client/src/bookingSlotsClient.ts`, либо
    - `apps/web/src/lib/bookingSlotsService.ts` + `apps/mobile/src/lib/bookingSlotsService.ts` на общей базе.
  - Инкапсулировать в нём:
    - вызов `get_free_slots_service_day_v2`,
    - нормализацию данных в `Slot[]` (единый тип),
    - базовую фильтрацию (время в будущем и т.п.).

- [x] 🟡 **CODE-SLOTS-2**: Унифицировать создание внутренних броней
  - Аналогично вынести логику `create_internal_booking` в сервис/клиент.
  - Использовать этот слой из:
    - QuickDesk,
    - staff‑slots UI,
    - (опционально) mobile, если там появится соответствующий сценарий.

**Предложение по решению (кратко):**
- Сначала реализовать общий сервис с существующей логикой (без изменения поведения).
- Затем последовательно заменить прямые RPC‑вызовы во всех компонентах на обращения к этому сервису.

---

## 3. Прямой Supabase в мобильных экранах (обход API/домена)

**Файл(ы):**
- `apps/mobile/src/screens/HomeScreen.tsx`
- `apps/mobile/src/screens/BookingDetailsScreen.tsx`
- другие экраны, использующие `supabase` напрямую

**Наблюдение:**
- Мобильные экраны сами знают структуру таблиц (`businesses`, `bookings` с вложенными связями), сами пишут фильтры и поиск.
- Одновременно часть операций (например, отмена брони) выполняется через HTTP‑эндпоинты и общий `apiRequest`.
- В результате логика web и mobile может расходиться, а mobile плотно завязан на схему Supabase и RLS.

**Задачи:**
- [x] 🟠 **CODE-MOBILE-SUPABASE-1**: Вынести ключевые чтения в HTTP‑API
  - Для бизнесов/списка броней/деталей брони:
    - добавить/уточнить соответствующие HTTP‑эндпоинты в `apps/web` (с опорой на домен),
    - описать DTO в `shared-client`.

- [x] 🟡 **CODE-MOBILE-SUPABASE-2**: Перевести мобильные экраны на общий API‑клиент
  - Использовать `createApiClient`/`shared-client` для чтения данных вместо прямых Supabase‑запросов.
  - Оставить Supabase на mobile только там, где это действительно необходимо (например, локальные/кэш‑сценарии, если такие будут).

**Предложение по решению (кратко):**
- Сначала описать необходимые DTO/эндпоинты и покрыть их базовыми тестами на web.
- Затем мигрировать mobile‑экраны с Supabase на эти эндпоинты, не меняя UI.

---

## 4. Жирный модуль `authBiz` и строковые коды ошибок доступа

**Файл(ы):**
- `apps/web/src/lib/authBiz.ts`

**Наблюдение:**
- В одном модуле смешаны:
  - создание Supabase‑клиентов (обычных и service‑role),
  - выбор текущего бизнеса (super‑admin, owner, staff),
  - синхронизация ролей `user_roles`,
  - логирование и диагностика.
- Ошибки доступа передаются через `Error('UNAUTHORIZED')` и `BizAccessError` со строковыми кодами.

**Задачи:**
- [x] 🟡 **CODE-AUTH-1**: Разбить `authBiz.ts` на специализированные модули
  - Например: `supabaseServerClient`, `bizContextResolver`, `staffRoleSync`, `authDiagnostics`.

- [x] 🟡 **CODE-AUTH-2**: Ввести типизированный enum/union для ошибок доступа
  - Определить ограниченный набор вариантов (`NOT_AUTHENTICATED`, `NO_BIZ_ACCESS`, `STAFF_ROLE_MISMATCH` и т.п.).
  - Использовать их вместо строк в `BizAccessError` и в вызывающих местах.

**Предложение по решению (кратко):**
- Сначала отделить чистые функции (выбор бизнеса, проверка роли) от инфраструктуры, затем ввести типизированные коды ошибок.

---

## 5. Доменные ошибки как `Error('<CODE>')` и сравнение по message

**Файл(ы):**
- `packages/core-domain/src/booking/useCases.ts`
- `apps/web/src/lib/errors.ts`

**Наблюдение:**
- В доменных use‑case’ах ошибки кодируются через `new Error('BRANCH_NOT_FOUND_OR_INACTIVE')` и подобные строки.
- В web‑слое тип ошибки определяется через `isErrorType(e, type)` c проверкой `e.message === type`.
- Это хрупко и не поддерживается компилятором.

**Задачи:**
- [x] 🟠 **CODE-DOMAIN-ERRORS-1**: Заменить строковые `Error` на типизированный результат
  - Ввести тип вида:
    - `type BookingErrorKind = 'BRANCH_NOT_FOUND_OR_INACTIVE' | 'NO_ACTIVE_BRANCH_FOR_BIZ' | ...;`
    - `type BookingError = { kind: BookingErrorKind; message?: string }`.
  - Возвращать из use‑case либо `Result<Success, BookingError>`, либо бросать специальный `BookingError`‑объект.

- [x] 🟡 **CODE-DOMAIN-ERRORS-2**: Обновить web‑слой на работу с типизированными ошибками
  - В `apps/web/src/lib/errors.ts` переключаться по `err.kind`, а не по `message`.
  - Привести API‑эндпоинты к единому формату JSON‑ошибок.

**Предложение по решению (кратко):**
- Начать с одного ключевого use‑case (например, cancel/mark‑attendance), внедрить новый формат ошибок, затем постепенно распространить на остальные.

---

## 6. Несоответствие уровня абстракции и утечка Supabase‑ошибок в `quick-hold`

**Файл(ы):**
- `apps/web/src/app/api/quick-hold/route.ts`

**Наблюдение:**
- Маршрут использует доменный use‑case, но:
  - в `commands` прямо передаёт Supabase‑клиент и RPC,
  - бросает `Error(error.message)` и строки вида `'Unexpected RPC result shape'`,
  - не всегда маппит ошибки в единый `ApiError`‑формат, в отличие от других эндпоинтов.

**Задачи:**
- [x] 🟡 **CODE-API-QUICK-HOLD-1**: Вынести Supabase‑команды в адаптер порта
  - Определить порт (интерфейс) для инфраструктурных операций, которые использует `createBookingUseCase`.
  - Реализовать этот порт в отдельном модуле (например, `apps/web/src/lib/bookingCommandsSupabase.ts`).

- [x] 🟡 **CODE-API-QUICK-HOLD-2**: Унифицировать маппинг ошибок в HTTP‑ответы
  - Использовать общий хелпер `createErrorResponse`/`ApiError` для всех веток ошибок.

**Предложение по решению (кратко):**
- Вынести весь Supabase‑специфичный код из `route.ts` в адаптер, а в самом route оставить только: парсинг запроса → вызов use‑case → маппинг результата в HTTP.

---

## 7. Смешение домена и Supabase в staff‑slots клиенте

**Файл(ы):**
- `apps/web/src/app/dashboard/staff/[id]/slots/Client.tsx`

**Наблюдение:**
- Компонент использует доменный `filterSlotsByContext`, но:
  - сам вызывает `get_free_slots_service_day_v2`,
  - сам вызывает `create_internal_booking`,
  - обрабатывает ошибки на уровне alert/простых сообщений.

**Задачи:**
- [x] 🟡 **CODE-STAFF-SLOTS-1**: Перевести staff‑slots на единый сервис слотов/броней
  - Использовать тот же слой, что будет реализован в задачах `CODE-SLOTS-1`/`CODE-SLOTS-2`.
  - Оставить компоненту только UI‑состояние и вызовы сервиса.

**Предложение по решению (кратко):**
- После появления общего сервиса слотов/броней переписать `Client.tsx` на его использование, максимально сохранив текущий UI.

---

## 8. Слабое UX ошибок для выбора слотов на mobile

**Файл(ы):**
- `apps/mobile/src/screens/booking/BookingStep5Time.tsx`

**Наблюдение:**
- Любая ошибка RPC по слотам обрабатывается как “общая ошибка”, по сути различаются только сетевые/офлайн‑кейсы.
- Доменные ошибки (например, филиал не активен, нет доступных слотов) не маппятся в понятные пользователю сообщения.

**Задачи:**
- [x] 🟡 **CODE-MOBILE-SLOTS-ERRORS-1**: Ввести типизированные ошибки/результаты для получения слотов
  - Использовать тот же контракт, что и для web‑слоя (через общий сервис слотов).
  - Маппить разные `error.kind` в разные user‑friendly сообщения/состояния экрана.

**Предложение по решению (кратко):**
- После появления типизированных доменных ошибок по слотам адаптировать `BookingStep5Time` так, чтобы он показывал пользователю конкретные причины отказа (нет слотов, филиал не работает и т.п.), а не просто “что‑то пошло не так”.

---

## 9. Критичный UI дашборда без таргетированных unit/интеграционных тестов

**Файл(ы):**
- `apps/web/src/app/dashboard/bookings/view.tsx`

**Наблюдение:**
- Много клиентской логики (фильтры, пресеты, быстрые действия, поиск).
- Jest‑тестов для этой логики нет; покрытие только через Playwright E2E.

**Задачи:**
- [x] 🟠 **CODE-TESTS-BOOKINGS-1**: Вынести и покрыть тестами чистую логику бронирований дашборда
  - Вынести редьюсеры/хелперы (фильтры, пресеты, маппинг состояний) в `apps/web/src/lib/dashboardBookingsLogic.ts` (или хуки).
  - Написать Jest‑тесты на эти функции/хуки.

**Предложение по решению (кратко):**
- Не пытаться “тестировать весь компонент целиком” — сначала выделить чистые функции/хуки и протестировать их отдельно, сохранив E2E для сквозной проверки.

---

## 10. Недостаточное тестовое покрытие сложных mobile‑флоу

**Файл(ы):**
- `apps/mobile/src/screens/HomeScreen.tsx`
- `apps/mobile/src/screens/BookingScreen.tsx`
- `apps/mobile/src/screens/booking/BookingStep1Branch.tsx`–`BookingStep6Confirm.tsx`

**Наблюдение:**
- Есть smoke‑тесты для Auth, Cabinet, Staff, BookingDetails, но:
  - нет тестов для `HomeScreen` (поиск, фильтры, оффлайн),
  - нет тестов для многошагового booking‑wizard при том, что логика там не тривиальная.

**Задачи:**
- [x] 🟡 **CODE-MOBILE-TESTS-1**: Добавить smoke‑тесты для `HomeScreen`
  - Проверить базовый рендер, отображение списка, работу поиска/фильтра, оффлайн‑баннер.

- [x] 🟡 **CODE-MOBILE-TESTS-2**: Добавить базовые тесты для booking‑wizard
  - Проверить happy‑path через шаги 1–6 с “идеальным” сценарием.
  - Зафиксировать хотя бы одно поведение по сохранению выбранных значений при переходах назад/вперёд.

**Предложение по решению (кратко):**
- Использовать текущую инфраструктуру Jest/React Native Testing Library и существующие примеры smoke‑тестов как шаблон, добавляя минимально необходимый набор кейсов.

---

## Как использовать этот документ

1. При планировании спринтов брать задачи с приоритетом 🔴/🟠, особенно из разделов 1–3 и 5.
2. При выполнении задачи:
   - обновлять статус чекбокса,
   - по возможности добавлять ссылки на PR/issue рядом с задачей.
3. Раз в квартал:
   - сверять этот документ с `IMPROVEMENT_TASKS.md` и фактическим состоянием кода,
   - переносить выполненные/неактуальные задачи, добавлять новые проблемные зоны.

