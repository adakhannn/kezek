# Kezek — дорожная карта улучшений

Документ для команды: список эволюционных задач по улучшению архитектуры, качества кода, DX и операционки.  
Критические проблемы уже закрыты (см. `DEEP_ANALYSIS_ISSUES.md`, `PROJECT_REVIEW.md`), здесь — следующий шаг развития.

Статусы:
- [ ] `todo` — задача запланирована
- [ ] `in-progress` — в работе
- [x] `done` — выполнено

Приоритеты:
- 🔴 критичный
- 🟠 высокий
- 🟡 средний
- 🟢 низкий

---

## 1. Доменный слой и архитектура

Цель: сделать доменную модель (бронирование, расписание, промо, финансы) ещё более независимой от Supabase/Next и удобной для повторного использования.

### 1.1. Выделение core-domain пакета

- [x] 🟠 **CORE-1**: Создать пакет `packages/core-domain`
  - Описать цель пакета в README (что сюда входит, а что нет)
  - Настроить экспорт через `index.ts` (booking, schedule, promotions, finance как модули)
  - Настроить сборку/типизацию как у `shared-client`

- [x] 🟠 **CORE-2**: Вынести доменные модели и сервисы бронирования
  - Описать `Booking`, `Branch`, `Staff`, `Service` как доменные типы (без привязки к Supabase типов)
  - Вынести в пакет `core-domain`:
    - валидацию параметров бронирования (`validateCreateBookingParams` и подобное)
    - работу с результатами RPC (`extractBookingId` и пр.)
  - Обновить API-роуты (`quick-hold`, `bookings/*`) на использование этих сервисов

- [x] 🟡 **CORE-3**: Оформить репозитории как интерфейсы
  - В `core-domain` ввести интерфейсы `BookingRepository`, `StaffRepository`, `BranchRepository`, `PromotionRepository`
  - В `apps/web` реализовать адаптеры поверх Supabase (RPC/таблицы)
  - Покрыть репозитории базовыми тестами (в т.ч. с использованием тестовой БД)

### 1.2. Application use-cases поверх домена

- [x] 🟡 **APP-1**: Ввести слой application use-cases
  - Для ключевых сценариев создать сервисы вида:
    - `CreateBookingUseCase`
    - `CancelBookingUseCase`
    - `ConfirmBookingUseCase`
    - `SendBookingNotificationsUseCase`
  - Вынести в них “оркестрацию”: вызовы домена + репозиториев + нотификаций

- [x] 🟡 **APP-2**: Упростить API-роуты до тонких адаптеров
  - Для нескольких ключевых эндпоинтов (например, `quick-hold`, `bookings/cancel`, `/api/notify`) переписать обработчики в виде:
    - HTTP → валидация (Zod) → вызов use-case → маппинг результата → HTTP-ответ
  - Зафиксировать паттерн в мини-гайде (HOWTO: новый API endpoint)

---

## 2. API-контракты и DTO

Цель: ещё сильнее закрепить стабильные контракты между backend и клиентами (web/mobile/интеграции).

- [x] 🟡 **API-1**: Явно задать versioning для публичного API
  - Описать в документации политику версионирования (например, `v1` как текущий “public surface”)
  - Добавить префикс/namespace в Swagger или хотя бы логический раздел “v1”

- [x] 🟡 **API-2**: Синхронизировать DTO и Zod-схемы
  - Для ключевых публичных эндпоинтов описать пары типов:
    - `XxxRequestDto`, `XxxResponseDto` (в `shared-client`)
  - Убедиться, что Zod-схемы в `apps/web` соответствуют этим типам (через `zod` + `z.infer` или явные маппинги)

- [x] 🟢 **API-3**: Автогенерация клиентского SDK по Swagger
  - Настроить генерацию TypeScript-клиента по существующей OpenAPI-схеме
  - Сравнить с текущим `createApiClient` из `shared-client` и при необходимости объединить подходы

---

## 3. Тесты: от процентов к сценариям

Цель: сместить фокус с “покрытия строк” к покрытию реальных бизнес-флоу.

- [x] 🟠 **TEST-1**: Составить реестр ключевых бизнес-сценариев
  - Составить список 10–20 сценариев (например: “публичная запись”, “запись по промо”, “отмена через WhatsApp”, “финансы сотрудника за день/неделю”)
  - Для каждого сценария указать:
    - задействованные эндпоинты
    - критичные ветки (happy path, edge cases)

- [x] 🟠 **TEST-2**: Перепривязать существующие тесты к сценариям
  - В документации к тестам (например, `TESTING_GUIDE.md`) добавить таблицу:
    - сценарий → какие Jest/E2E тесты его покрывают
  - Отметить сценарии, которые пока не покрыты или покрыты слабо

- [x] 🟡 **TEST-3**: Добить минимум по E2E для сценариев
  - Для каждого ключевого сценария обеспечить хотя бы один E2E-тест (web или mobile — в зависимости от приоритета)
  - Постепенно поднимать порог покрытия (не только % по Jest, но и % покрытых сценариев)

---

## 4. DX и процессы разработки

Цель: снизить “входной порог” для новых задач и сделать изменения более предсказуемыми.

- [x] 🟡 **DX-1**: Шаблоны (генераторы) для новых фич
  - Добавить генератор (plop/hygen/свой скрипт) для:
    - нового API-роута (`route.ts` + Zod-схема + тест + запись в Swagger)
    - новой страницы/экрана (Next page + базовый тест)
    - нового доменного use-case
  - Задокументировать использование (короткий HOWTO)

- [x] 🟢 **DX-2**: HOWTO “новая сущность”
  - Написать пошаговый гид “Как добавить новую сущность (пример: Coupons)”:
    - миграция Supabase
    - CRUD API
    - страница/раздел в админке
    - тесты (SQL + API + E2E)
    - мониторинг/логирование

---

## 5. Мониторинг и операционка

Цель: не только собирать метрики/ошибки, но и иметь чёткую обзорную картину состояния системы.

- [x] 🟡 **OPS-1**: Свести мониторинг в единую “операционную панель”
  - Определить, где живёт основная дашка (Sentry, внешний мониторинг, другой инструмент)
  - Вывести туда:
    - ошибки (Sentry)
    - latency/ошибки по ключевым эндпоинтам
    - web-vitals
    - базовые бизнес-метрики (кол-во броней, конверсия hold→confirm, всплески отмен)

- [x] 🟢 **OPS-2**: Стандартизировать health-check
  - Расширить/уточнить `/api/admin/health-check`:
    - секции: Supabase, Resend, WhatsApp/Telegram, Redis, file storage (если появится)
    - понятный JSON-формат “ok / degraded / fail”
  - Задокументировать, как использовать health-check во внешнем мониторинге

---

## 6. Каналы коммуникации (email / WhatsApp / Telegram / будущее)

Цель: иметь одну расширяемую платформу уведомлений.

- [x] 🟡 **NOTIFY-1**: Абстракция NotificationChannel
  - Вынести контракт уведомлений в общий модуль (например, в `shared-client` или `core-domain`):
    - `NotificationChannel` (email, whatsapp, telegram, push)
    - базовые структуры сообщений (текст, шаблон, переменные)

- [x] 🟡 **NOTIFY-2**: Единая модель предпочтений
  - Чётко определить, где хранится информация:
    - какие каналы доступны у бизнеса
    - какие каналы предпочитает клиент
  - Обновить `NotificationOrchestrator`, чтобы он опирался на эти настройки

---

## 7. UX и мобильные сценарии

Цель: сделать мобильный сценарий записи и работы с записями максимально устойчивым и удобным.

- [x] 🟢 **MOB-1**: Карта мобильных сценариев
  - Составить список ключевых сценариев мобильного приложения (авторизация, запись, просмотр историй, отмена, взаимодействие с WhatsApp-сообщениями)
  - Для каждого сценария описать:
    - зависимости от сети / API
    - какие есть фолбэки (ошибки сети, падение Supabase и т.п.)

- [x] 🟢 **MOB-2**: Базовый оффлайн-режим для просмотра своих броней
  - Продумать и описать (пока на уровне дизайна), как кешировать брони и давать пользователю минимум функционала без сети
  - Зафиксировать это в отдельном документе (например, `MOBILE_OFFLINE_DESIGN.md`)

---

## 8. Следующий этап (Phase 2)

Задачи этого блока — развитие уже заложенных идей (core-domain, use-cases, сценарии, мобильный оффлайн) в сторону “production‑ready” состояния.

### 8.1. Архитектура: завершить переход на use-cases

- [x] 🟠 **ARCH-1**: Переписать `bookings/[id]/cancel` на `cancelBookingUseCase`
  - Вынести оркестрацию в use-case: проверка прав, вызов RPC/репозитория, нотификации (если нужны)
  - Оставить в API-роуте только HTTP-обвязку (auth, Zod, маппинг ошибок)

- [x] 🟠 **ARCH-2**: Переписать `bookings/[id]/mark-attendance` на use-case
  - Выделить use-case для сценария “клиент пришёл / no‑show / отмена в последний момент”
  - Вынести бизнес-правила (штрафы, промо, перерасчёт часов и финансов) из API-роута

- [x] 🟠 **ARCH-3**: Переписать `staff/shift/close` на use-case
  - Реализовать use-case закрытия смены с пересчётом часов и финансов (с опорой на сценарии из `SCENARIO_TEST_REGISTRY.md`)
  - Оставить в API только валидацию и маппинг на HTTP

- [x] 🟡 **ARCH-4**: Вынести `/api/notify` на use-case уведомлений
  - Использовать `sendBookingNotificationsUseCase` и абстракции из `NOTIFICATIONS_PREFERENCES.md`
  - Подготовиться к добавлению новых каналов (push, in‑app)

### 8.2. Качество и мобильный опыт

- [x] 🟠 **Q-1**: Добить тесты по критичным сценариям 1–5
  - Для сценариев 1–5 из `SCENARIO_TEST_REGISTRY.md` убедиться, что есть как минимум один Jest и один E2E‑тест
  - Обновить мини‑матрицу соответствия сценариев и тестов

- [x] 🟡 **MOB-3**: Начать реализацию оффлайн‑кэша для “моих броней”
  - По `MOBILE_OFFLINE_DESIGN.md` реализовать минимальный кэш броней в мобильном приложении (read‑only)
  - Добавить пользовательские состояния для offline/online и конфликтов данных

---

## 9. Как использовать этот документ

1. При планировании спринтов выбирать задачи из разделов с приоритетом 🔴/🟠.
2. При выполнении задачи:
   - обновлять статус чекбокса,
   - при необходимости ссылаться на PR/issue.
3. Раз в квартал:
   - ревью документа вместе с `PROJECT_REVIEW.md` и `DEEP_ANALYSIS_ISSUES.md`,
   - добавлять новые задачи или убирать устаревшие.

