# Решение проблем с переменными окружения (Mobile)

Инструкции по устранению проблем с загрузкой переменных окружения (Expo не видит `.env.local` и т.п.) перенесены в единый гайд.

👉 **[ENV_GUIDE.md](../../ENV_GUIDE.md)** — раздел **«2.5. Если переменные не подхватываются (troubleshooting)»**: формат файла, путь, кэш `--clear`, проверка логов и альтернативы.

Кратко: убедитесь, что файл `apps/mobile/.env.local`, переменные с префиксом `EXPO_PUBLIC_`, без пробелов вокруг `=`, и перезапустите `npx expo start --clear`.
